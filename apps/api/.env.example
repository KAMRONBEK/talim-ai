# API secrets live in Doppler (project: talim-ai).
# Run from repo root: pnpm doppler:setup && pnpm dev
#
# Required keys — see /env.template:
#   PORT, DATABASE_URL, REDIS_URL, JWT_SECRET, DEEPSEEK_API_KEY,
#   OPENAI_API_KEY, UPLOAD_DIR, CORS_ORIGIN
