# Talim AI — User Stories & QA Traceability

This is the **durable spec + results ledger**. It is *not* a run journal — that's
[`visual-qa-report.md`](./visual-qa-report.md). Here, every user story is decomposed to
its deepest edge cases, and each edge case carries a **live status**, the **finding** it
produced, and the **fix** that closed it. Read this to answer "is behaviour X tested, and
does it work?" at any moment.

> **Backlog expansion:** [`user-stories-expansion.md`](./user-stories-expansion.md) holds **76**
> additional deep stories (~1,470 edge cases) mined from a full code read — AI video, slides,
> assessments, the admin panel, the multi-tenant isolation matrix, a11y, background jobs, the
> quota matrix and cascade-deletes. Promote stories from there into this ledger as they are
> verified. That pass also surfaced **90 suspected bugs** (its own ledger); the confirmed +
> fixed ones are recorded below as **F32–F39**.

---

## How to read this

- **Story** = one slice of value for one role: `US-<AREA>-<n>`. Areas: `AUTH`, `IND`
  (individual/B2C), `OWNER` (tenant owner), `LEARNER` (tenant learner), `ADMIN`, `XCUT`
  (cross-cutting: i18n, a11y, mobile, security).
- **Acceptance criteria (AC)** = the happy-path contract, Given/When/Then.
- **Edge cases (EC)** = every negative path, boundary, race, and "what if" we can think of.
  *This is where the value is* — the deeper the list, the better the QA. Add ECs as you
  discover them; an EC that "can't happen" still gets logged with reasoning.
- **Finding** = link to an `F<n>` row in the [Findings ledger](#findings-ledger).
- **Fix** = the commit SHA that closed it (so the result is auditable from git).

### Status legend
| Symbol | Meaning |
| --- | --- |
| ✅ | Tested, passes |
| ❌ | Tested, fails → must have a linked `F#` |
| 🟡 | Partially tested / works with caveat |
| 🐛→✅ | Failed, finding logged, **fix verified** |
| ⏭️ | Deferred (with reason) |
| ⬜ | Not yet tested |
| 🚫 | N/A by design (record *why*) |

### Severity (for findings)
`S1` blocker (data loss, security, role-isolation breach, crash) ·
`S2` major (flow broken, wrong result) · `S3` minor (cosmetic, copy, untranslated) ·
`S4` polish.

---

## Story template (copy this for each new story)

```markdown
### US-<AREA>-<n>: <short title>
**As a** <role>, **I want** <goal>, **so that** <benefit>.
**Routes/code:** <url(s)> · <key files>
**Priority:** P0/P1/P2 · **Last verified:** <date> on <branch/commit>

**Acceptance criteria**
- AC1 — Given <state>, When <action>, Then <result>.
- AC2 — …

**Edge cases & negative paths**
| # | Scenario | Expected behaviour | Status | Finding | Fix |
| --- | --- | --- | --- | --- | --- |
| EC1 | … | … | ⬜ | — | — |

**Notes / open questions**
- …
```

---

## Worked examples (the depth to aim for)

### US-AUTH-01: Email/password login
**As a** registered user, **I want** to log in, **so that** I reach my role's workspace.
**Routes/code:** `/[locale]/login` · `apps/api .../auth` · role redirect in web middleware.
**Priority:** P0 · **Last verified:** 2026-06-25 on `59dc681` (run 3: EC4/6/7/10/11/12/14)

**Acceptance criteria**
- AC1 — Given valid credentials, When I submit, Then I land on my role's home
  (INDIVIDUAL→`/dashboard`, LEARNER→`/learner/dashboard`, OWNER→`/tenant/dashboard`,
  ADMIN→`/login` on 3001→dashboard).
- AC2 — Given I was deep-linked while logged out, When I log in, Then I return to that link.
- AC3 — The session persists across reload and the active locale is preserved.

**Edge cases & negative paths**
| # | Scenario | Expected behaviour | Status | Finding | Fix |
| --- | --- | --- | --- | --- | --- |
| EC1 | Wrong password | "Invalid email or password" (not "server unreachable") | 🐛→✅ | F2 | — |
| EC2 | Unknown email | Same generic message as EC1 (no user enumeration) | ✅ | — | — |
| EC3 | **Deactivated** account logs in | 403 → "account deactivated" message, not "server unreachable" | 🐛→✅ | F16 | `d5a13cc` |
| EC4 | Email-less kid logs in by **username** | Resolves `username@students.talim.local`, succeeds | ✅ | — | `teststudent1`→/learner/dashboard, 200 |
| EC5 | Kid with `mustChangePassword` | Forced to change-password screen before workspace | ⬜ | — | — |
| EC6 | Email with leading/trailing spaces | Trimmed, login succeeds | ✅ | — | `"  qa-owner@…  "` trims, logs in |
| EC7 | Email/username differing only in case | Case-insensitive match, succeeds | 🐛→✅ | F17 | `59dc681` |
| EC8 | Rate limit after N failed attempts | Clear "too many attempts" message, not a 500 | ⏭️ | — | needs 30 attempts; deferred |
| EC9 | XSS/SQL payload in fields | Escaped, no execution, generic error | ✅ | — | XSS-escape verified |
| EC10 | Very long inputs (10k chars) | Rejected gracefully, no crash | ✅ | — | 10k email/pw → 401, no 500 |
| EC11 | Locale switch then login | Lands in the chosen locale, not default | ✅ | — | uz selected → /uz/tenant/dashboard |
| EC12 | Logged-in user revisits `/login` | Redirected to their home, not shown the form | ✅ | — | bounces to /learner/dashboard (brief hydration flash) |
| EC13 | Session expiry mid-session | Bounced to `/login`, return-after-login honoured | ⬜ | — | — |
| EC14 | Submit with empty fields | Native required validation, no network call | ✅ | — | native "Please fill out this field."; no /auth/login call |

**Notes / open questions**
- Register has **no confirm-password field** → password-mismatch EC is 🚫 N/A (logged in run 1).

### US-OWNER-12: Delete a material
**As a** tenant owner, **I want** to delete a material, **so that** stale content is removed
and unassigned from students.
**Routes/code:** `/tenant/materials` · delete dialog component.
**Priority:** P1 · **Last verified:** 2026-06-25 on `claude/visual-qa`

**Acceptance criteria**
- AC1 — Given a material, When I delete and confirm, Then it's removed and its assignments drop.
- AC2 — A confirmation dialog appears before any destructive action.

**Edge cases & negative paths**
| # | Scenario | Expected behaviour | Status | Finding | Fix |
| --- | --- | --- | --- | --- | --- |
| EC1 | Delete dialog copy + delete aria-label | Translated per locale (was hardcoded Uzbek) | 🐛→✅ | F15 | `36f1f41` |
| EC2 | Delete a material **assigned** to N students | Assignments removed, students no longer see it | ⬜ | — | — |
| EC3 | Cancel the dialog | Nothing deleted, dialog closes | ⬜ | — | — |
| EC4 | Delete material mid-generation (podcast/quiz job running) | No orphaned job / graceful handling | ⬜ | — | — |
| EC5 | Delete then a learner is mid-chat on it | Learner's access revoked cleanly, no crash | ⬜ | — | — |
| EC6 | Another owner's material (cross-tenant) via crafted ID | 403 via `contentAccess.service.ts`; never deletable | ⬜ | — | **S1 — isolation** |
| EC7 | Double-click delete / double submit | Single delete, no duplicate error | ⬜ | — | — |

### US-LEARNER-01: Sees only assigned materials (multi-tenant isolation)
**As a** tenant learner, **I want** to see only the materials my tutor assigned me, **so that** my
classmates' and other orgs' content stay private.
**Routes/code:** `/learner/dashboard` · `GET /content`, `GET /content/:id(/*)` · `contentAccess.service.ts` (`assertCanAccessContent`, `buildContentListWhere`).
**Priority:** P0 (S1 isolation) · **Last verified:** 2026-06-25 on `4a0a57a` (live, run 4)

**Acceptance criteria**
- AC1 — The dashboard + `GET /content` return only content assigned to *me* while my membership is active.
- AC2 — Any per-record access to content I'm not assigned returns 404 (no existence leak), via the central guard.

**Edge cases & negative paths** — verified live with each learner's real bearer token (crafted `fetch`).
| # | Scenario | Expected behaviour | Status | Finding | Fix |
| --- | --- | --- | --- | --- | --- |
| EC1 | Assigned learner dashboard | Shows only assigned content (1 article) | ✅ | — | `teststudent1`: 1 assigned |
| EC2 | `GET /content/:id` for **own assigned** content | 200 (control) | ✅ | — | — |
| EC3 | `GET /content/:id` for **B2C** content (`tenantId=null`) via crafted id | 404 "Content not found" | ✅ | — | cross-boundary blocked |
| EC4 | `GET /content/:id` for **another student's** content in the **same tenant** (unassigned) | 404 — never leaks | ✅ | — | **S1**: `teststudent2`→`teststudent1`'s id = 404 |
| EC5 | `GET /content/:id/file` (sub-resource) for unauthorized content | 404 (sub-paths guarded too) | ✅ | — | — |
| EC6 | `GET /content/:id` for a garbage / nonexistent id | 404, no crash | ✅ | — | — |
| EC7 | `GET /content` list as an **unassigned** learner | `contents:0` (empty) | ✅ | — | `teststudent2`: 0 |
| EC8 | UI navigate to an unauthorized content URL | Redirect to `/learner/dashboard`, no hang/leak | ✅ | — | F8 fix holds |
| EC9 | Cross-tenant learner via crafted id (other org) | 404 | ⬜ | — | other tenants have no content yet |
| EC10 | Deactivated mid-session then access (same token) | 404/403 immediately (not at JWT expiry) | ✅ | — | live: content 200→404, list→0, `/learner` 403; reactivate restores |
| EC11 | **Unassigned mid-view** (workspace open, owner revokes the ContentAssignment) | Access lost immediately on the same token; open page redirects to `/learner/dashboard` on next action, no hang/leak | ✅ | — | run 13 live: deleted assignment → content 200→**404**/file 404/list→0 (no JWT wait); reload → clean redirect to dashboard (F8 holds mid-session); restored |

**Notes / open questions**
- The guard keys on a `ContentAssignment` for *this* learner **and** an active membership — same-tenant-unassigned and cross-boundary both correctly 404.

### US-LEARNER-04: Cannot reach owner/admin tools (role guard)
**As the** platform, **I want** a learner blocked from owner/admin routes, **so that** privilege escalation is impossible.
**Routes/code:** `/tenant/*` (`requireTenantOwner`), `/admin/*` (`requireRole('ADMIN')`), web RoleGuard.
**Priority:** P0 (S1) · **Last verified:** 2026-06-25 on `4a0a57a` (live, run 4)

**Edge cases & negative paths**
| # | Scenario | Expected behaviour | Status | Finding | Fix |
| --- | --- | --- | --- | --- | --- |
| EC1 | Learner → `GET /tenant/content` / `/tenant/students` | 403 Forbidden | ✅ | — | both 403 |
| EC2 | Learner → `GET /admin/users` / `/admin/tenants` | 403 Forbidden | ✅ | — | both 403 |
| EC3 | Learner UI navigate to `/tenant/dashboard` | Redirect to `/learner/dashboard` | ✅ | — | — |
| EC4 | Learner own routes `/learner/assessments`, `/usage/me` | 200 (control) | ✅ | — | — |
| EC5 | Learner upload via B2C workspace topbar | No upload control rendered (incl. hidden file input) | ✅ | — | F7+F13 fixed prior runs |

### US-AUTH-03: Join-code enrolment + seat limits
**As a** student, **I want** to self-enrol in my tutor's class with a join code, **so that** I get their materials — but never past the paid seat limit.
**Routes/code:** `/[locale]/register` (+joinCode) · `POST /auth/join-class` · `joinTenantByCode` · `assertTenantQuota('STUDENT')`.
**Priority:** P0 (billing boundary) · **Last verified:** 2026-06-25 on `4978bb3` (run 5)

**Edge cases & negative paths**
| # | Scenario | Expected behaviour | Status | Finding | Fix |
| --- | --- | --- | --- | --- | --- |
| EC1 | Valid join code at register | Enrols as TENANT_LEARNER in that org | ✅ | — | run 2 (qa-joincode) |
| EC2 | Seat limit full — **enforcement** | Enrol blocked, 402 QUOTA_EXCEEDED (no over-enrol past paid seats) | ✅ | — | both `/register`+joinCode and `/join-class` call `assertTenantQuota('STUDENT')` |
| EC3 | Seat-full **error message** | "Seat limit reached", not "Upload limit reached" | 🐛→✅ | F26 | `4978bb3` |
| EC4 | Seat-full via **register** (account side-effect) | No orphaned account; clean failure | ❌ | F27 | — (structural, logged) |
| EC5 | Already an active member re-joins | Idempotent (no extra seat consumed) | ✅ | — | code: returns early if `existing.active` |
| EC6 | Owner tries to join own/other class | 400 "Tutors cannot join a class" | ✅ | — | code-verified guard |
| EC7 | Reactivating an inactive membership | Consumes a seat (re-checks quota) | ✅ | — | code: `if (!existing?.active) assertTenantQuota` |

### US-IND-05: Podcast — generate + player
**As an** individual, **I want** an AI voice podcast of my content with a working player, **so that** I can listen.
**Routes/code:** `/[locale]/content/[id]/podcast` · `components/podcast/PodcastPlayer.tsx` · `generatePodcast` job.
**Priority:** P1 · **Last verified:** 2026-06-25 on `5adc666` (run 5, qa-individual PDF, en/dark)

**Edge cases & negative paths**
| # | Scenario | Expected behaviour | Status | Finding | Fix |
| --- | --- | --- | --- | --- | --- |
| EC1 | INDIVIDUAL "Create podcast" → generation | Episodes stream in (per section), Ready badge, TTS audio | ✅ | — | 2 episodes generated, ep1 Ready 2:31 |
| EC2 | Play an episode while/after generating | Audio plays, position advances; no blob churn / console spam | 🐛→✅ | F21 | `46e2473` |
| EC3 | Player "Speed:" label localized | en "Speed:", not hardcoded "Tezlik:" | 🐛→✅ | F22 | `5adc666` |
| EC4 | Learner views podcast page | Info message, **no** Create button (server-blocked) | ✅ | — | F12 (prior run) holds |
| EC5 | Episode list duration vs player duration | Should match | 🟡 | — | list "2:31" vs player "2:16" — minor estimate mismatch, not fixed |
| EC6 | Speed buttons 0.75/1/1.25/1.5x, ±15s, seek | All adjust playback | ⬜ | — | controls present; play verified, fine-controls not each clicked |

### US-IND-03 / 04 / 06: B2C workspace — Summary, Quiz, Chat (PDF)
**As an** individual, **I want** AI summary, quizzes, and a region-scoped tutor on my PDF, **so that** I can study it.
**Routes/code:** `/[locale]/content/[id]` · `/quiz/[id]` · chat panel · `components/learning/*`, `components/quiz/*`.
**Priority:** P0 · **Last verified:** 2026-06-25 on `b4ba377` (run 5, qa-individual PDF "Ven diagrammasi 2-qism.pdf", uz)

**Edge cases & negative paths**
| # | Story · Scenario | Expected behaviour | Status | Finding | Fix |
| --- | --- | --- | --- | --- | --- |
| EC1 | IND-03 · Summary auto-generates on Xulosa toggle | Renders fluent proper-Uzbek summary (3 paras), persists to history | ✅ | — | accurate Venn/perimeter summary |
| EC2 | IND-03 · summary content quality | No raw dumps / hallucinated UI | 🟡 | — | one garbled source word "masquniyoq" (model/OCR artifact, not UI) |
| EC3 | IND-04 · Quiz generate from PDF | Valid MC + short-answer, proper Uzbek, good distractors | ✅ | — | 4 Qs (button said 5 — AI count variance) |
| EC4 | IND-04 · short-answer Check reveal | "To'g'ri!" + Uzbek explanation, **no `<div>`-in-`<p>` hydration error** | ✅ | — | F4 regression holds (console clean) |
| EC5 | IND-04 · submit → score → retry | "50% · 2/4 to'g'ri" + Qayta ishlash | ✅ | — | — |
| EC6 | IND-06 · **PDF marquee region → chat seed** | Drag region → "[Page 1] Tanlangan hudud" chip + Uzbek prompt; answer scoped to region (vision) | ✅ | — | pending since run 1 — now verified |
| EC7 | IND-06 · chat history persists across tab/view switch | Conversation retained when toggling Material/Xulosa & Learn/Tutor tabs | ✅ | — | — |

### US-XCUT-01: i18n — every user-facing string localized, Uzbek-first
**As an** Uzbek-first user, **I want** every string/date/number correctly localized in uz/en/ru, **so that** the product reads natively with no raw keys, English leaks, or broken formatting.
**Routes/code:** `apps/web/messages/{uz,en,ru}.json` · `lib/format-relative-time.ts` · any `Intl.*` / `toLocale*` call.
**Priority:** P1 · **Last verified:** 2026-06-25 on `b4ba377`

**Acceptance criteria**
- AC1 — Every visible string resolves in all 3 locales; no raw keys (`content.foo`), no hardcoded English/Uzbek leaking across locales.
- AC2 — Dates, relative times, numbers, and plurals render correctly **including Uzbek** (where ICU data is thin).

**Edge cases & negative paths**
| # | Scenario | Expected behaviour | Status | Finding | Fix |
| --- | --- | --- | --- | --- | --- |
| EC1 | Relative timestamp in **uz** (content card, history) | Uzbek words ("3 hafta oldin"), not raw "-3 w" | 🐛→✅ | F18 | `b4ba377` |
| EC2 | Relative timestamp in en/ru | "3 weeks ago" / "3 недели назад" | ✅ | — | Intl correct for en/ru |
| EC3 | `toLocaleDateString()` (no locale arg) on tenant progress/students/heatmap | Renders a valid date, but in **system** locale not app locale | 🟡 | — | works; not app-locale-aware (low pri) |
| EC4 | Material delete dialog + aria-label across locales | Translated (was hardcoded Uzbek) | 🐛→✅ | F15 | `36f1f41` |
| EC5 | Login/marketing strings in uz/ru | Fully translated, no English leak, layout holds | ✅ | — | verified runs 1–2 |
| EC6 | Count strings pluralized (ru paucal, en singular) | ICU plural per locale: ru "4 раздела", en "1 section" | 🐛→✅ | F20 | `aa42bf1` |

**Notes / open questions**
- ICU in V8/Node lacks Uzbek data for `RelativeTimeFormat` (and likely thin for `DateTimeFormat`/`PluralRules`) — any new `Intl`-based formatting for `uz` must be checked manually (see F18).

### US-IND-08: Usage limit → subscription promotion modal
**As** a FREE individual who hits a usage limit, **I want** a clear upgrade prompt **so that** I know how to unblock myself.
- **AC:** every quota-gated action returning 402 `QUOTA_EXCEEDED` (with `upgradePlanCode: INDIVIDUAL_PRO`) or 413 `PLAN_FILE_LIMIT` opens the single global promotion modal with a feature-specific headline; the hard 120 MB cap (`FILE_TOO_LARGE`) shows an **inline** message, not the modal (upgrading wouldn't lift it); tenant-owner / already-Pro limits show an inline message, not the individual modal (role + `upgradePlanCode` branch in `useLimitErrorHandler`).
- **EC matrix (verified run 7 — Playwright, qa-individual on FREE with daily limits temporarily set to 0):**
  - EC1 **UPLOAD** (dashboard upload) → modal "today's upload limit" ✅
  - EC2 **GENERATION** (practice quiz) → modal "today's AI generation limit" ✅
  - EC3 **PODCAST** (per-episode regenerate) → modal "today's podcast limit" ✅
  - EC4 **VIDEO** (generate part) → modal ✅ (the video controller checks GENERATION before VIDEO, so the headline reads "generation")
  - EC5 **TUTOR_MESSAGE** (chat send) → modal "today's tutor message limit"; empty assistant placeholder removed on error ✅
  - EC6 **PLAN_FILE_LIMIT** (30 MB file vs FREE 25 MB cap) → modal "too big for the Free plan" + "100 pages / 25 MB" ✅
  - EC7 **FILE_TOO_LARGE** (130 MB > 120 MB hard cap) → **inline** "maximum upload size is 120 MB", no modal ✅
  - EC8 (logic) tenant-owner / already-Pro → inline message, no individual modal (no self-serve Pro path)
- **Files:** `lib/limit-error.ts`, `hooks/useLimitErrorHandler.ts`, `store/useUpgradeModal.ts`, `components/account/{global-upgrade-modal,upgrade-dialog}.tsx`, `lib/pricing.ts`, the wired pages/hooks (upload/youtube/quiz/summary/video/podcast/chat). See FEATURES.md §6.8.

---

## Story index (backlog — expand each with the template)

> Tick `spec'd` when the story has full ACs + EC matrix; `done` when all P0/P1 ECs are ✅/🐛→✅/🚫.

### AUTH
- [x] US-AUTH-01 Login · spec'd ✅
- [ ] US-AUTH-02 Register (valid / duplicate email / weak pw / join-code self-enroll)
- [x] US-AUTH-03 Join-code enrol: valid / wrong / **seat-limit full** / already-member · spec'd ✅ · seat-limit enforced (both paths); F26 fixed, F27 logged (run 5)
- [ ] US-AUTH-04 Reset password (request → email/link → set → re-login)
- [ ] US-AUTH-05 Logout (clears session, redirect, back-button can't re-enter)
- [ ] US-AUTH-06 Become-tutor request → admin approval → role unlock

### INDIVIDUAL (B2C)
- [ ] US-IND-01 Upload PDF → processing → READY → workspace (+ OCR scanned-PDF ladder)
- [ ] US-IND-02 Add YouTube → transcript → READY
- [x] US-IND-03 Summary (markdown, KaTeX, proper Uzbek output) · spec'd ✅ · PDF summary verified (run 5)
- [x] US-IND-04 Quiz generate → MC/short → check → submit → retry · spec'd ✅ · PDF quiz verified (run 5)
- [x] US-IND-05 Podcast generate + player · spec'd ✅ · generation + playback verified (run 5); F21/F22 fixed
- [x] US-IND-06 Chat: streamed, scoped-to-material, sources, seeding from selection, visual tutor · spec'd ✅ · **PDF marquee seed verified (run 5)**; transcript-seed+mermaid+KaTeX (run 2)
- [x] US-IND-07 Dashboard grid / empty / search / thumbnails · search filter verified (run 5, F19 logged)
- [x] US-IND-08 Usage limit → subscription promotion modal (upload / gen / podcast / video / tutor + plan-file cap) · spec'd ✅ · all 7 cases verified live (run 7); F31 fixed
- [x] US-IND-19 Slides deck — DeckPlayer render + nav · **live render verified (run 13)** (5-slide deck, prev/next/fullscreen/progress/aria-live); **F57 focus-ring + F60 Regenerate-no-op fixed**. Note: decks are **per-locale** (uz showed the generate-empty-state — by design, like podcast/video)

### TENANT_OWNER
- [ ] US-OWNER-01 Create student (email + email-less kid, credentials-once, seat count)
- [ ] US-OWNER-02 Reset student password
- [ ] US-OWNER-03 Deactivate / reactivate student
- [ ] US-OWNER-04 Join code regenerate (old rejected) / copy
- [ ] US-OWNER-05 Upload material / re-read OCR
- [ ] US-OWNER-06 Assign material to student(s) · F58 fixed (run 13): multi-assign now continues on a per-learner failure + shows error
- [ ] US-OWNER-07 Question bank build + approve (proper Uzbek, LaTeX)
- [ ] US-OWNER-08 WRITTEN assessment create + assign + grade · F56 fixed (run 13): assigning a DRAFT assessment now blocked (400)
- [ ] US-OWNER-09 GAME assessment (timer, speed points, streaks, leaderboard)
- [ ] US-OWNER-10 Progress: per-student + class, post-submit update
- [ ] US-OWNER-11 Billing / seat-limit display
- [ ] US-OWNER-12 Delete material · spec'd ✅
- [ ] US-OWNER-13 Settings / org rename

### TENANT_LEARNER
- [x] US-LEARNER-01 Sees **only assigned** materials (isolation) · spec'd ✅ · done (P0 ECs green)
- [x] US-LEARNER-02 Take quiz / game, see own progress · GAME played end-to-end in uz (run 5); F23 fixed (player+leaderboard i18n)
- [x] US-LEARNER-03 **Deactivated → content access lost immediately** (S1 isolation) · login-side (F16) + live mid-session (US-LEARNER-01·EC10) ✅
- [x] US-LEARNER-04 Cannot upload / cannot reach owner tools (role guard) · spec'd ✅ · done

### ADMIN (3001)
- [ ] US-ADMIN-01 Tutor-requests: approve + set seat limit → org + ACTIVE subscription
- [x] US-ADMIN-02 Users / tenants / content / generated / subscriptions / usage / audit · user-detail + **subscription patch + audit** verified (run 5); F25 fixed (credential autofill)
- [x] US-ADMIN-04 Tenant detail — org/sub editor, members table (XSS-escaped, long-name wrap), usage-vs-limits · verified live (run 13)
- [x] US-ADMIN-05 Content moderation — **Retry** (FAILED→PROCESSING→READY recovered + `content.retry_job` audit) + **Delete** (native confirm → 204 → live removal + `content.delete` audit) · verified live (run 13, closes Runs 7–11 deferral); F52 holds
- [x] US-ADMIN-06 Usage & costs — per-user spend table + 7d/30d/90d range toggle (refetches `?days=`) · verified live (run 13)
- [x] US-ADMIN-07 Audit log — newest-first, all mutation types + from/to metadata · re-validated (run 13)

### XCUT (cross-cutting)
- [x] US-XCUT-01 i18n: every user-facing string in uz/en/ru, no hardcoded leaks (Uzbek-first) · spec'd ✅
- [ ] US-XCUT-02 Mobile (drawer/FAB) + tablet (768) layouts
- [ ] US-XCUT-03 a11y: focus, aria-labels, keyboard nav, back/forward · F48/F49 (run 9); **F55 mobile-Sheet focus-trap + F57 deck focus-ring fixed (run 13)**
- [ ] US-XCUT-04 Security: role isolation via `contentAccess.service.ts`, XSS escape, no enumeration
- [ ] US-XCUT-05 Resilience: SSR errors, stale-cache, slow network, double-submit

---

## Findings ledger

Central registry. Each row links to the story/EC that produced it and the fix that closed it.
Backfill F1–F14 from `visual-qa-report.md` as you revisit them.

| F# | Sev | Story · EC | Summary | Status | Fix commit |
| --- | --- | --- | --- | --- | --- |
| F84 | S4 | US-ADMIN · health · EC (new) | **Audit trail overstated deep-check provider spend.** `POST /admin/health/deep` inside the 60s cooldown replays a **cached** report and spends nothing, but still wrote an audit row carrying the *original* run's `durationMs` — indistinguishable from a real run. Oracle (local DB counts): one real run then three immediate re-POSTs → `health.deep_check` count **5→8**, all four rows `durationMs=2423`, while response `cached=true` proved only the first executed. An auditor would read four deep checks = 4× provider spend. **Fix:** keep logging every request (who asked, when) but add `servedFromCache` / `spentProviderCalls` to the metadata. Verified live: fresh run → `spentProviderCalls:true`; immediate re-POST → `servedFromCache:true, spentProviderCalls:false`. | 🐛→✅ | `921b8fd8` |
| F83 | S3 | US-ADMIN · health · EC (new) | **Health-verdict changes were silent to screen readers.** `/health` (:3001) re-polls every 60s (`refetchInterval: 60_000`) and swaps the verdict text **in place**, but the banner was a plain `<p>` and there was **no live region anywhere in the document** (verified document-wide: `[aria-live],[role=status],[role=alert],output` → **0 elements**, twice, incl. from fresh state after a real reload). So a non-sighted operator monitoring the page was never told when the verdict flipped from "ALL SYSTEMS GO" to broken — the single event the page exists to surface. Oracle: DOM-structural (`verdictMutatesInPlace: true` + `liveRegionCount: 0`); skeptic pass ruled out a global toaster/portal region. **Fix:** verdict block → `role="status" aria-live="polite" aria-atomic="true"` (queues politely, announces verdict+counts as one sentence); unreachable banner → `role="alert"` (interrupts); verdict icon `aria-hidden`. Verified live: region announces *"2 ISSUES \| 22 checks passed · 2 not set up · checked just now"*. Status was already text-not-colour (OK/DEGRADED/NOT SET UP) and both actions keyboard-operable (tabIndex 0) — unchanged. | 🐛→✅ | `e19ce5d7` |
| F64 | S2 | US-IND-22 · EC | **AI tutor ignored chat history — follow-up questions got a canned "please clarify".** Asking an in-scope question, then a follow-up like *"koproq tuwunting, chizib tushuntiring"* ("explain more, draw it") or *"oxirgi yechilgan masalni visual tushuntirib bering"* ("explain the last solved problem visually") returned the static **"Savolingizni biroz aniqlashtirib bera olasizmi?"** clarification instead of an answer. **Root cause:** the scope gate `classifyTutorScope` (`lib/tutor-scope.ts`) runs *before* the tutor LLM and was **stateless** — it saw only the current message + its RAG context, never the conversation, so anaphoric follow-ups had no referent → `needs_clarification` → controller short-circuits with the canned reply; the tutor (which *does* get history) never ran. **Also found:** `chat.controller.ts` fetched history with `orderBy: asc, take: 20` = the **20 oldest** messages, so long sessions lost recent memory. **Fix:** thread the recent (refusal/clarification-stripped) turns into both the LLM classifier (+ explicit follow-up instruction) and the heuristic fallback (`looksLikeFollowUp`); fetch the most-recent 20 chronologically. Smoke test extended with both reported follow-ups (now `direct`). Verified live via real `/chat/stream`: in-scope Q → answer; follow-up → 509-char answer referencing *"yuqoridagi diagramma"* (not the clarification). | 🐛→✅ | `da1174c` |
| F63 | S2 | US-IND-06 · EC | **Selecting a PDF region opened a duplicate Learn/AI-tutor panel on desktop.** `handleExcerpt` (`content/[id]/page.tsx`) called `setPanelOpen(true)` unconditionally on a marquee region-select; the `ContentLearnPanelSheet` (mobile Learn drawer) is rendered at every breakpoint, so on desktop it slid open **over** the already-visible `ContentLearnPanel` — two identical Learn panels. (First seen as a non-repro observation in Run 2.) Gated `setPanelOpen(true)` to mobile only (`matchMedia('(max-width: 767px)')`), mirroring the existing `?panel=chat` effect; on desktop the region now just seeds the visible panel's AI-Tutor tab. Verified live (1440px): marquee select → 0 dialogs, no backdrop, AI-Tutor tab active, excerpt chip seeded. | 🐛→✅ | `d52558f` |
| F60 | S3 | US-IND-19 · EC4 | **Slide-deck "Regenerate" was a silent no-op once a deck existed.** `createSlides` (slides.controller.ts) short-circuited to the cached READY deck unconditionally, so the Regenerate button (`generate.mutate`) returned the same deck (`cached:true`) and never regenerated. Added a `regenerate` flag that skips the cache short-circuit; the button passes it. Verified live: control (no flag) → `cached:true`; `regenerate:true` → `cached:false`, fresh 5-slide deck in 37s. (The audience-cache-key part — `@@unique([contentId,locale,scopeKey])` lacks `audience`, and there is no audience selector in the UI — is a separate migration-bearing enhancement, deferred.) | 🐛→✅ | `a5680a6` |
| F59 | S2 | US-IND-24 · EC1 | **Quiz generation that returns 0 questions / FAILS spun forever — no FAILED state.** The `Quiz` model has **no `status` field**, so a 0-question or failed `generateQuiz` job had nowhere to persist FAILED; `useQuiz` polled until `questions.length` (never, on failure) and `QuizCard`/quiz page rendered the "generating…" spinner **indefinitely**. **Fixed (no-migration):** `isQuizGenerationStale` — 0 questions + older than the 120s generation window → flips to a "Quiz generation failed — go back and try again" state (uz/en/ru) + a re-render timer; self-corrects when questions arrive and never false-flags a still-generating quiz. Verified live: backdated empty quiz → failed state; fresh empty quiz → "generating"; deleted both. (A persisted `Quiz.status` remains the ideal robust fix — needs a migration, still logged as the enhancement.) | 🐛→✅ | `a3d2be3` |
| F58 | S2 | US-OWNER-06 · EC6 | **Multi-assign aborted silently on one failing learner.** `assign-students-panel.tsx` `handleAssign` awaited each assignment in a loop with **no try/catch** — if one learner rejected (e.g. deactivated since the panel loaded — a stale-cache race; backend `assignContent` 404s on an inactive membership) the whole loop threw, skipping the rest, with **no error toast** and the selection cleared/button stuck. Now each assignment is independent; failed ids stay selected for retry and an inline `assign.partialError` (uz/en/ru, ICU plural) shows. Verified live: selected a student → deactivated via API → Assign → "Couldn't assign to 1 student. Please try again." + student stayed selected. | 🐛→✅ | `f9e8652` |
| F57 | S2 | US-XCUT-03 · EC8 | **Slide-deck player had no visible keyboard focus ring.** `DeckPlayer.tsx` focusable carousel root used `outline-none` with no `focus-visible` replacement (and the prev/next/fullscreen buttons had none) — keyboard users got no focus indicator (WCAG 2.4.7). Added `focus-visible:ring` (inset primary on the root, offset ring on buttons). Verified live on the 5-slide deck: keyboard focus renders a 2px inset primary ring `rgb(119,81,236)`. | 🐛→✅ | `a3bcd85` |
| F56 | S2 | US-OWNER-08 · EC6 | **A DRAFT assessment could be assigned.** `assessment/assessments.ts` `assignAssessment` checked only existence, not `status` — a DRAFT is filtered out of the learner's PUBLISHED-only list (`learner.ts`) and 404s on submit, so an owner could create a **dead assignment with no signal**. Now 400 "Assessment must be published before it can be assigned". Verified live: flip to DRAFT → assign 400; PUBLISHED control → 201; restored. | 🐛→✅ | `1be7528` |
| F55 | S2 | US-XCUT-03 · a11y | **Mobile Sheet drawer was not a real modal dialog.** Hand-rolled `packages/ui/components/sheet.tsx` (Menyu/Learn drawers, tenant/dashboard sidebars) had **no `role=dialog`/`aria-modal`, no initial focus, no focus trap (Tab leaked to the page behind the backdrop), no Escape-to-close, no focus restore, no scroll-lock** — confirmed live (Tab → background logo; Escape no-op). Added full dialog semantics + Tab-trap-with-wrap + Escape + focus-restore + scroll-lock + `aria-labelledby` from the first heading. Verified live (375px): focus moves in, Tab wraps inside, Escape closes + restores focus to trigger, body scroll locked/unlocked. | 🐛→✅ | `b433ea4` |
| F54 | S3 | US-XCUT-02 · tablet | Marketing navbar links pill `md:flex` overflowed at 768px and **clipped the "Get started" CTA**; gated to `lg:flex` so the bar shows logo+toggle+Kirish+Boshlash at tablet (run 11). | 🐛→✅ | `c520bb6` |
| F53 | S3 | US-XCUT-02 · mobile | Marketing **hero clipped at 390px** — the hero grid lacked a base `grid-cols-1`, so the mobile column sized to the product card's max-content (419px in a 342px container), clipping the headline/subtitle/card (all 3 locales; `overflow-hidden` hid the scroll). Added `grid-cols-1` (run 11). | 🐛→✅ | `4d5652a` |
| F52 | S2 | US-ADMIN-05 · audit | **`POST /admin/contents/:id/retry-job` wrote no audit row** (delete-content/delete-generated do) — re-enqueueing a stuck job was invisible in the audit log. Added `content.retry_job` audit + a `req.user` guard. Verified live. | 🐛→✅ | `dbf9f4e` |
| F51 | S2 | US-ADMIN-03 · audit | **`PATCH /admin/users/:id` did not audit non-role edits.** Only role changes wrote `user.role_change`; a name / `preferredLocale` / the sensitive plaintext `adminPasswordNote` edit persisted with NO audit row — breaking "every admin action recorded". Now also writes `user.update` (field names only, never the note value). Verified live: role-change/reset-pw/subscription/delete already audited; name+note edit → `user.update`. | 🐛→✅ | `d3bcd3c` |
| F48 | S2 | US-XCUT-03 · a11y | **Two `<select>` on `/tenant/assessments` had no accessible name** (axe `select-name`, critical); added `aria-label` from each section title. Re-audit 2→0. | 🐛→✅ | `0d51248` |
| F49 | S3 | US-XCUT-03 · a11y | **Dashboard content-card thumbnail link had no discernible text** (axe `link-name`); added `aria-label={content.title}`. Re-audit 1→0. | 🐛→✅ | `0d51248` |
| F50 | S3 | US-XCUT-03 · a11y | **Active sidebar nav fails color-contrast** (`text-primary` on `bg-primary/10`, < 4.5:1); app-wide design-system decision → logged. | 🟡 logged | — |
| F46 | S2 | US-AUTH-04/05 · session | **No session revocation on password change / logout.** Tokens are stateless 7-day JWTs with no `tokenVersion`/denylist, so changing the password (or logging out) does NOT invalidate existing tokens — confirmed live: old token still 200s on `/auth/me` after a password change. A leaked/stolen token survives a reset for the full token lifetime (logout is client-side only). **Fix (structural, shared with F45):** add a `User.tokenVersion`, embed it in the JWT, bump on password/role change, reject stale versions in `authMiddleware`. Needs auth-path + perf review. | 🟡 logged | — |
| F45 | S2 | US-AUTH-06 · approval | **Stale JWT after a role change (tutor approval).** Admin approval flips `User.role` INDIVIDUAL→TENANT_OWNER in the DB, and `/auth/me` (DB-reloaded) returns OWNER so the web routes to `/tenant/dashboard` — but the user's existing JWT still encodes `role:INDIVIDUAL`, so **every `/tenant/*` API call 403s until they log out and back in** (`authMiddleware` only backfills role for legacy tokens). Confirmed live: approve → `/auth/me` OWNER, `/tenant/students` with old token **403**, re-login → 200. **Fix (structural, NOT auto-applied):** have `me()` reissue a fresh token when the DB role ≠ the JWT role and store it in `session-sync` (or add a `tokenVersion`); needs auth-path review + testing. | 🟡 logged | — |
| F44 | S3 | US-OWNER-01 · concurrency | **Concurrent create-student raced to a 500.** Two simultaneous identical creates (double-click) both passed the `findUnique` pre-checks then collided on the unique email/username constraint; the Prisma **P2002 was uncaught → 500**. Now caught → **409** "Username already taken". Verified: 3 parallel creates 201/500/500 → 201/409/409. (Sequential dup + case-variant already returned clean 409s.) | 🐛→✅ | `27f6ac6` |
| F43 | S2 | US-AUTH-02 · EC13 | **Orphaned account on register-with-invalid-join-code** (broadened F27 from seat-full to ANY join failure). `register()` created the user + FREE sub before `joinTenantByCode`, so an invalid code 404'd after the account existed; the email was then stuck on retry. Pre-validate the code's tenant exists before `user.create`. Verified live: invalid code → 404, no account, re-register succeeds. | 🐛→✅ | `0379da8` |
| F42 | S3 | US-LEARNER-06 · EC6 | **Forced password change could be satisfied with the same password.** `changePassword` had no new≠current check, so a `mustChangePassword` student could "change" to the tutor-set secret and clear the flag without rotating. Now 400 "New password must be different from the current password". Verified live. | 🐛→✅ | `0169859` |
| F41 | S3 | US-IND-10 · EC15 | **Upload-error toast showed hardcoded English "Upload failed" on uz/ru.** `useFileUpload` fell back to a literal `'Upload failed'`; the dashboard quick-action + learning-topbar callers don't pass a localized message (only `UploadCard` does), so non-English users saw English on every failed upload. Defaulted the fallback to `t('content.uploadFailed')` inside the hook. Verified live (uz): "Yuklash amalga oshmadi. Qayta urinib ko'ring." | 🐛→✅ | `a80ddad` |
| F40 | S3 | US-IND-10 · EC22 | **Upload picker still offered PowerPoint after F35 rejected it.** `FILE_UPLOAD_ACCEPT = '.pdf,.ppt,.pptx'` invited users to pick a `.ppt/.pptx` the server now 400s — a UI/server mismatch. Narrowed `accept` to `.pdf`. | 🐛→✅ | `a80ddad` |
| F32 | S2 | US-IND-25 · podcast | **A single per-episode podcast regenerate failure marked the WHOLE podcast FAILED** — the shared Bull `failed` handler ignored `episodeId` and forced `status:FAILED`, destroying an otherwise-READY podcast whose other episodes were intact. Now episode-scoped: recompute status from surviving audio (READY if any episode still has audio). | 🐛→✅ | `e0a8846` |
| F33 | S2 | US-OWNER-11 · billing | **TRIALING tenant subscription was fully locked out** — `requireActiveTenantSubscription` 402'd every non-ACTIVE status, so a trial org couldn't upload / add students / generate. TRIALING now counts as active access; PAST_DUE/CANCELED still block. | 🐛→✅ | `e0a8846` |
| F34 | S2 | US-LEARNER-06 | **`mustChangePassword` was not enforced** — surfaced only as a *dismissible* welcome banner, so a student could use the whole app on a temporary password. Added a learner-shell route gate forcing `/learner/settings` until changed (store flag cleared on success). Verified live: flagged student bounced to settings (stable, no loop); normal learners unaffected. | 🐛→✅ | `15acc73` |
| F35 | S2 | US-IND-01 · upload | **PowerPoint (.ppt/.pptx) uploads ALWAYS failed** — accepted by multer, then `processContent` routed `SLIDE` through `pdf-parse` (PDF-only), which throws on a pptx ZIP (no extractor exists). Now rejected at the upload boundary with a clear 400 ("export to PDF and upload that"). | 🐛→✅ | `e0a8846` |
| F36 | S3 | US-IND-23 · chat | **Chat mid-stream server error rendered the raw English "Stream failed"** (un-localized on uz/ru) and didn't persist (ghost message on reload). Now flagged → routed through the outer catch (removes the optimistic bubbles) → ChatWindow surfaces the localized chat error + restores the composer. | 🐛→✅ | `15acc73` |
| F37 | S2 | US-ADMIN-03 · sub | **Admin cancel of an individual subscription rewrote `planId`→FREE**, so a later re-ACTIVATE returned FREE (the paid plan was lost); the tenant path kept it. CANCELED already gets free-plan limits at read time, so now only the status is written. | 🐛→✅ | `e0a8846` |
| F38 | S2 | US-IND-20 · OCR | **`POST /content/:id/ocr-region` (paid vision OCR) had no rate limit** (unlike `/reparse`) — a cost-abuse vector. Added `reparseRateLimit` (access was already guarded by `blockLearnerMutations` + `assertCanAccessContent`). | 🐛→✅ | `e0a8846` |
| F39 | S2 | US-OWNER-09 · GAME | **GAME leaderboard speed-points are computed from client-supplied per-question `timings`** (trusted), so a learner can POST `timings:0` to force `speedFactor=1.0` and inflate points. A correct fix needs a server-measured clock (persist a per-attempt/served timestamp) → logged, not fixed. | 🟡 logged | — |
| F31 | S3 | US-IND-08 | **Usage-limit errors mostly didn't surface the upgrade modal.** Only file uploads (PLAN_FILE_LIMIT) opened it; daily quota errors (UPLOAD/GENERATION/PODCAST/VIDEO/TUTOR_MESSAGE) failed silently or with ad-hoc inline text — the tutor stream threw an unhandled rejection (empty assistant bubble left behind), and video/podcast/quiz/summary had no or partial 402 handling. Built a unified `classifyLimitError` + role-aware `useLimitErrorHandler` + one global `useUpgradeModal`/`GlobalUpgradeModal` (mounted in `providers.tsx`) and wired every quota-gated call site. Self-serve INDIVIDUAL limits → modal; tenant / top-plan / hard 120 MB cap → inline. Verified all 7 cases live (run 7). | ✅ fixed | _pending commit (this session)_ |
| F30 | S3 | US-IND-05 · EC | **Podcast per-episode regenerate (+ overall retry) gave no error feedback.** The new per-episode "Qayta urinish" button (and the stuck/failed retry button) called the regenerate/create mutation but never surfaced its rejection — a 402 quota (FREE plan, podcast quota 1/1 already spent by the bulk generation) returned silently, so the button looked like it did nothing. Added `classifyGenerationError` → visible header message ("Podkast cheklovi tugadi {used}/{limit}" for quota; plan/generic otherwise). Verified via Playwright: FREE regenerate → 402 → "Podkast cheklovi tugadi (1/1)." | ✅ fixed | `b861405` |
| F15 | S3 | US-OWNER-12 · EC1 | Material delete dialog + aria-label hardcoded Uzbek | ✅ fixed | `36f1f41` |
| F16 | S2 | US-AUTH-01 · EC3 | Deactivated login showed "server unreachable" not "deactivated" | ✅ fixed | `d5a13cc` |
| F17 | S2 | US-AUTH-01 · EC7 | Email/username login was **case-sensitive** — any capitalization difference (mobile auto-capitalize) → "Invalid email or password", user locked out of a P0 flow. Register also stored email verbatim. Fixed: lowercase+dedupe email on register; case-insensitive (`mode:'insensitive'`) email & username match on login. | ✅ fixed | `59dc681` |
| F18 | S2 | US-XCUT-01 · EC1 | **Uzbek relative timestamps rendered broken.** `Intl.RelativeTimeFormat('uz')` resolves to `uz` but ICU (V8/Node) ships **no Uzbek relative-time data**, so it emitted raw fallback `"-3 w"` / `"-2 d"` / `"-5 h"` (leading minus + English abbreviations) on **every content card timestamp** — shown to the **primary Uzbek audience** on the B2C dashboard + learning-history panel. en/ru correct. Fixed: format Uzbek manually (`"3 hafta oldin"`, `"hozirgina"`, future `"3 kundan keyin"`); keep `Intl` for en/ru. Verified live: card now reads "3 hafta oldin". | ✅ fixed | `b4ba377` |
| F29 | S3 | US-XCUT-01 · EC7 | **Learner progress page hardcoded English.** 7 strings (Progress / My progress / "Your activity and assigned materials." / Learning streak / "{n} days" / Average quiz / empty-state copy) shown verbatim on uz/ru. Added `learner.progress` namespace (uz/en/ru), reused `assignedMaterials`+`streakDays`, wired `useTranslations`. Verified live: uz page zero English leaks ("TARAQQIYOT" / "Mening taraqqiyotim" / "O'rganish ketma-ketligi" / "1 kun"). **Remaining learner-area i18n debt (logged, not fixed):** learner Settings page, learner assessments **list** page (F24), tenant `/tenant/*` pages. The 3 highest-traffic learner pages (dashboard F28, progress F29, GAME player F23) are now localized. | ✅ fixed | `65e2b73` |
| F28 | S3 | US-XCUT-01 · EC7 | **Learner dashboard hardcoded English** (every student's landing page, Uzbek-first). ~11 strings leaked to uz/ru: stat labels (Assigned/Streak/Avg quiz), streak "N days", "Your teacher will assign materials here", Tasks section (title/"View all"/"N questions · used/max attempts"), and the "Continue where you left off" card (title/"N% complete"/"Continue learning") + "Your school" fallback. Added `learner.*` keys (uz/en/ru, ICU plural for streak/questions) + wired via the existing `useTranslations('learner')`. Verified live: uz dashboard has **zero** English leaks. | ✅ fixed | `295cdc0` |
| F26 | S3 | US-AUTH-03 · EC | **Seat-limit-full reported as "Upload limit reached".** When a tenant's student seat limit is hit (owner adding a student, or a learner self-enrolling via join code), `subscription/tenant.ts` threw `QuotaExceededError('UPLOAD', …)` because `QuotaFeature` had no `STUDENT` member — so the API/UI showed the misleading "Upload limit reached" (`feature:UPLOAD`) for a full class. Added a `STUDENT` QuotaFeature + "Seat limit reached" message; threw it from the tenant STUDENT branch. Verified live: full-class register → 402 `{message:"Seat limit reached", feature:"STUDENT"}`. | ✅ fixed | `4978bb3` |
| F27 | S2 | US-AUTH-03 · EC | **Orphaned account when register-with-join-code hits a full class.** `POST /auth/register` creates the user **before** calling `joinTenantByCode`, so when the class is seat-full the join throws 402 *after* the account already exists. The user sees "Seat limit reached" and assumes registration failed, but their account was created as a plain **INDIVIDUAL** (verified: login as the email succeeds, role INDIVIDUAL, tenantId null) — they're not in the class, and retrying the same email now hits "Email already registered". Fix is structural (validate the join code + seat quota *before* creating the user, or make create+join atomic) → logged, not fixed. | 🟡 logged | — |
| F25 | S2 | US-ADMIN-02 · EC | **Admin user-detail credential fields silently browser-autofilled.** On `/users/[id]`, the "Password note (backfill)" and "Set new password" inputs had no `autoComplete` guard, so Chrome **silently pre-filled the operator's own saved login** (`admin@talim.local` / `Talim-655ed15296ab`) into them on every page load (verified `:autofill = true`); the "Recorded password" display even reflected the autofilled note, making it look like the *target* user had that password. Clicking "Set password"/"Save note" would then overwrite the target user's password with the admin's own — a credential-leak + silent password-change. Fixed: `autoComplete="off"` on the note, `autoComplete="new-password"` on the set-password input. Verified live: both fields now empty, `:autofill = false`. | ✅ fixed | `73e41c9` |
| F23 | S3 | US-LEARNER-02 · EC | **GAME quiz player + leaderboard hardcoded English** (CLAUDE.md-flagged debt). `game-quiz-player.tsx` + `leaderboard-table.tsx` rendered ~15 English literals (intro meta, Start/Cancel, Scoring, Your score, result summary, Your answer/Correct, Done, Question N/M, Number/Your-answer placeholders, Next, No scores, pts) — shown to Uzbek students in the marquee GAME feature. Added `learner.game` namespace (uz/en/ru, ICU plural for ru points/questions) + `useTranslations`. **Verified live**: full game played in uz (intro "4 ta savol…", "1 / 4-savol", "SIZNING BALLINGIZ", "Tayyor", leaderboard "1510 ball"), 0 console errors. | ✅ fixed | `e57e4ef` |
| F24 | S3 | US-XCUT-01 · EC7 | **Assessments pages largely un-i18n'd (hardcoded English).** The tenant assessments admin page (`/tenant/assessments`) and the learner assessments **list** page (`/learner/assessments`) render English on uz/ru: "Assessments", "Question banks", "Publish assessment", "Mode"/"Written"/"Game", "Max attempts", "Assign", "Results & leaderboard"; learner list "Quizzes & tasks", "Play", "Leaderboard", "Attempts: N/M · Latest X% · N pts", "Attempt limit reached", "Hide leaderboard". The GAME *player* + *leaderboard table* are now localized (F23); these surrounding list/admin pages are a larger remaining surface → logged, not fixed. | 🟡 logged | — |
| F21 | S2 | US-IND-05 · EC2 | **Podcast playback broken + blob-404 spam.** The audio-loading effect (`podcast/page.tsx`) depended on `flushProgress` — a `useCallback` over the react-query mutation, so a new identity every render. While the podcast polled (3s) during generation the parent re-rendered constantly, re-running the effect: it revoked the current audio blob URL and created a new one **every render**, spamming `blob: ERR_FILE_NOT_FOUND` (10+/play) and resetting `<audio>` to 0 so `play()` never stuck (`paused:true, t:0`). Scoped the effect to the audio episode id via a stable `flushProgressRef` + `cancelled` guard. Verified live: src stable across poll cycles, playback advances (`paused:false, t:1.52`), console 0 errors. | ✅ fixed | `46e2473` |
| F22 | S3 | US-IND-05 · EC3 | **Podcast player "Speed:" label hardcoded Uzbek.** `PodcastPlayer.tsx` rendered the literal `"Tezlik:"`, shown on en/ru pages too. Added `content.playbackSpeed` (uz/en/ru) + `useTranslations`. Verified: en "Speed:". | ✅ fixed | `5adc666` |
| F20 | S3 | US-XCUT-01 · EC6 | **Count strings not pluralized (Russian + English).** `sectionCount`/`quizCount`/`questionCount`/`episodes`/`quizAttempts` hardcoded the genitive-plural suffix (`"{count} разделов"`, `"{count} sections"`) instead of ICU `plural`. Russian showed `"4 разделов"` (needs paucal `"4 раздела"`; "разделов" is 5+ only) and English `"1 sections"`. Fixed all 5 in en (`one`/`other`) + ru (`one`/`few`/`many`/`other`) using next-intl ICU `plural`; uz left unchanged (Uzbek nouns invariant after numerals). Verified live: ru "4 раздела"/"2 вопроса"/"5 вопросов", en "4 sections". | ✅ fixed | `aa42bf1` |
| F19 | S3 | US-IND-07 · EC | **Dashboard search "no results" shows the "no content yet" empty state.** Typing a non-matching term in the dashboard hero search (client-side filter of the recents grid) empties the list and renders "Hali material yo'q. …birinchi materialingizni qo'shing" ("You have no materials yet, add your first") — confusing for a user who *does* have content but filtered it out. Should show a distinct "no results match your search" state. Not fixed: needs a new string in uz/en/ru + grid logic to distinguish filtered-empty from truly-empty (product copy decision). | 🟡 logged | — |
| … | | | *(backfill F1–F14 here)* | | |


---

# Coverage-expansion additions (2026-07-12) — reservations, observations ledger, index

## B.1 — Findings-ledger reservation (insert as a note above the ledger table + reserved rows)

> **Reserved block F76–F99** for the coverage-expansion pass (US-IND-26→34, US-LEARNER-14→18,
> US-OWNER-18→25, US-ADMIN-08b/10/11, US-XCUT-22→24). Claim numbers **in order** as findings are
> confirmed via the §E self-verification protocol; do not reuse F1–F75. A finding is only assigned
> an F-number after reproduce-twice + evidence-bundle + skeptic pass. Everything unverified or
> preference-level goes to the **Observations ledger (`O<n>`)** below, not here.

| F# | Sev | Story · EC | Summary | Status | Fix commit |
| --- | --- | --- | --- | --- | --- |
| F76 | S3 | US-OWNER-20 · due-date | Assign due-date hint copy (uz/en/ru) + `assessment/shared.ts:168` schema comment both said the date "does not block submission", but `submitLearnerAssessment` (`learner.ts:97`) hard-**403s** late submits for WRITTEN + GAME. Reproduced live (past dueAt → 403). Corrected copy + comment to match enforcement. | 🟢 fixed | R18 (branch) |
| F77 | S3 | US-OWNER-18 · assign | `assignAssessment` (`assessments.ts:133-136`) does `if (existing) continue` — re-assigning an already-assigned learner silently no-ops (`201 {assignments:[]}`), so a **due date / content-scope set on re-assign is dropped**, and (no unassign route) the due date can't be changed once assigned. Needs a product decision on upsert semantics. | 🟡 logged | — |
| F78 | S3 | US-ADMIN-11 · EC3 | Flagging generated media is **label-only** — `GeneratedMediaReview{status:FLAGGED}` is written + shown in admin `/generated` but **zero** consumers on any learner/serving path, so a FLAGGED podcast/quiz/slideshow is still fully served. **R21 re-confirmed LIVE (was grep-only):** source grep for `GeneratedMediaReview`/`FLAGGED` hits only schema+migration+`admin/content.controller.ts` (no serving-path consumer); live A/B on the podcast (`cmqx0f75r…`) assigned to teststudent1 — learner served **6 episodes**, admin flagged it FLAGGED (admin list confirms), learner re-fetch → **200, still 6 episodes, no under-review marker in body**; restored APPROVED. Flag has zero serving effect. | 🟡 logged (R21 live-reconfirmed) | — |
| F79 | S3 | US-OWNER · csv-export | Students-roster CSV export escaped only RFC-4180 (quote/comma/newline), **not** formula injection (CWE-1236): a student name / `@username` starting with `= + - @ TAB CR` executes as a formula in Excel/Sheets. Names are user-controlled (self-enroll / owner / CSV import). Prefixed formula-leading cells with `'`; verified live end-to-end. | 🟢 fixed | R18 (branch) |
| F80 | S3 | US-XCUT · i18n-ru | `ru.json` `becomeTutorPromo` + `readyToLearnSubtitle` (B2C dashboard) were **Russian written in Latin transliteration** ("Upravlyayte uchenikami…", "Dobavte istochnik…") instead of Cyrillic — garbled/off-brand for the secondary-priority language. Deep 3-file sweep confirmed only these 2 (key-parity 1287/1287/1287, 0 missing). Corrected to Cyrillic; verified live on `/ru/dashboard`; typecheck trio green. | 🟢 fixed | R19 (branch) |
| F81 | S4 | US-IND · podcast | Podcast (audio) transcript click-to-seek hint read "**Videoning** shu joyiga o'tish…" (says *video* on an `<audio>` player) — the shared `TranscriptPanel` (podcast + video viewer) hardcoded one `transcriptClickToSeek` key = "video" in all 3 locales. Added `mediaKind='video'|'audio'` prop (default video → video viewer unchanged) + `transcriptClickToSeekAudio` (uz/en/ru); `PodcastPlayer` passes `audio`. Verified live ("Audioning…"); typecheck trio green; key-parity 1288³. Was O83. | 🟢 fixed | R20 (branch) |
| F85 | S2 | US-XCUT · error-states | **A failed request is rendered as a confirmed-empty state**, so the app makes a positive false claim about the user's own data instead of admitting a failure. Three sites, each oracle-checked against the API: `/uz/dashboard` — `GET /content` 200 with **2 materials**, UI says "Hali material yo'q"; `/uz/learner/assessments` — `GET /learner/assessments` 200 with **2** ("QA Written Quiz!", "QA Game Quiz"), UI says "Hali hech narsa tayinlanmagan."; `/uz/tenant/students` — `GET /tenant` 200 with `joinCode:"DUTDWE"`, card says "Kod yo'q". All three: `[role=alert],.text-destructive,[data-error]` → **0**, no error text, **no spinner** — a stable, confident lie with no retry control. Cause: `isError` is never consumed and `undefined` is defaulted to `[]` (`dashboard/page.tsx:26,29-30,65-70` → `recent-content-grid.tsx:113-120`; `learner/assessments/page.tsx:887`; `join-code-card.tsx:14,26`). Consequence: a student skips homework, a tutor regenerates a join code they already handed out, a B2C learner re-uploads and burns quota. Reproduced twice, 2nd from fresh state. Structural (many call sites) → filed, not fixed. | 🟡 filed | [#40](https://github.com/KAMRONBEK/talim-ai/issues/40) |
| F86 | S3 | US-LEARNER · deactivation | **A deactivated student is never told** — access is correctly revoked but the app keeps running and just reports emptiness. Mid-session deactivation → dashboard shows "0 ta material biriktirildi", the first-login banner still invites them to set a password, a deep-link to their previously-assigned material silently bounces to the dashboard, and **nothing** matches `/faolsiz\|deactivat\|o'chirilgan\|bloklangan/i` anywhere in the page. The honest message exists only behind a manual sign-out (`POST /auth/login` → 403 "Student account is deactivated"). Cause: `apps/web/lib/api.ts:37` globally handles **401 only**; the deactivated learner's 403s are unclaimed, so the session is never ended. **Promoted from O86** (first seen R19, 2026-07-14 — sat unbundled ~6 weeks). Security itself is sound: verified on an already-issued token, content 200→404, `/learner/materials` 200→403, `/learner/assessments` 200→403, immediately. | 🟡 filed | [#41](https://github.com/KAMRONBEK/talim-ai/issues/41) |
| F87 | S2 | US-OWNER · seats · **SECURITY — details withheld** | **Standing invariant "seat limit is never exceeded" is violated.** Sequential enforcement is correct on all three seat-consuming paths (single create, CSV import, join-code register all refuse with `402 QUOTA_EXCEEDED` at zero free seats), but it does **not** survive concurrency. Runtime-confirmed and magnitude-measured this run; reproduced twice from clean state. This repo is public, so the reproduction, the affected paths and the overshoot figures live **only** in the draft advisory — do not restate them here. Raised the existing advisory from static-analysis `low` to `medium`. Probe accounts purged and `seatLimit` restored to its original value after the test. | 🔒 advisory | [GHSA-2r57-gmq8-w83r](https://github.com/KAMRONBEK/talim-ai/security/advisories/GHSA-2r57-gmq8-w83r) |
| F88 | S2 | US-LEARNER · assessments · **SECURITY — details withheld** | **`maxAttempts` is not enforced under concurrency** — a one-shot assessment can be submitted and **graded** repeatedly. Sequential enforcement is correct (a lone follow-up submission returns `409 "Attempt limit reached"`), so the check exists and only atomicity is missing. Runtime-confirmed and **reproduced twice**, the second time on a freshly created assessment from clean state. Student-side cheat vector needing no privilege beyond being the assigned learner, and every extra attempt is recorded GRADED, feeding Elo-KT mastery and the class leaderboard. Raised the existing advisory from static-analysis `low` to `medium`. Repo is public → reproduction and figures live **only** in the advisory. | 🔒 advisory | [GHSA-9m6m-jwgc-r8wp](https://github.com/KAMRONBEK/talim-ai/security/advisories/GHSA-9m6m-jwgc-r8wp) |
| F89 | S2 | US-LEARNER · game · **SECURITY — details withheld** | **GAME timing is not server-authoritative** — the standing invariant "GAME/assessment timing is server-authoritative" is violated. Runtime-confirmed this run: two attempts with **byte-identical answers** differing only in self-reported timings produced a **2.00× point swing**, and the forged attempt took rank 1 on the class leaderboard read back through the learner API. The attempt was submitted after a deliberate 35 s real idle against a 20 s-per-question limit and still scored maximum speed points. The clamp's default also penalises honest clients (omitting timings scores the *worst* factor), so the incentive points the wrong way. Compounds with F88. Repo is public → reproduction and figures live **only** in the advisory. Severity left at `medium` — the confirmation makes it actionable, not more dangerous. | 🔒 advisory | [GHSA-mvr4-9xv9-6585](https://github.com/KAMRONBEK/talim-ai/security/advisories/GHSA-mvr4-9xv9-6585) |
| F90 | S3 | US-LEARNER · game · a11y | **The timed GAME quiz is silent to assistive technology.** Document-wide `[aria-live],[role=status],[role=alert],output,[aria-atomic]` → **0**, no `role="timer"`, measured at three moments during live play — while a 20 s countdown ticks, the speed bonus visibly decays (962 → 938 ballgacha), the question counter advances (`1/3` → `2/3`) and answer feedback appears, all swapped **in place**. Same class as F83, fixed on admin `/health` in `e19ce5d7`; the player never got the same treatment. **Passed** in the same pass and not worth re-testing: keyboard-operable end to end (whole game played with Tab/Enter), focus visible (3px outline + ring), **0/14** inputs truly unlabeled. Filed not fixed — `aria-live` on a per-second countdown would announce every tick, so the fix needs a design decision. WCAG SC 4.1.3 failed; SC 2.2.1 raised as a question (real-time/essential exception is arguable for a game). | 🟡 filed | [#42](https://github.com/KAMRONBEK/talim-ai/issues/42) |
| F91 | S3 | US-ADMIN · users · **FLAKY** | **One-off `500` on `GET /admin/users/:id` left the admin detail page stuck on "Loading user…"** (13 chars of main text, no error affordance, no retry). Caught by the full sweep; **did not reproduce** — 25 sequential + 12 concurrent GETs on the same id all 200, and no other probe user returned ≥400. Recorded as F-flaky rather than an O because it is S2-shaped (a 500 that blanks a page) and the flaky rule exists for exactly this. Leading hypothesis: same signature as **O84** — a transient 500 under API saturation (this landed ~270 page-loads into a 545 s sweep; API and Bull worker share a process). Caveat: the id the sweep picked was a soft-deleted account created by this run's own concurrent-import race, so the trigger may involve state only this run produced. Incidental: that user is absent from `GET /admin/users?limit=200` yet served 200 by the detail route. **RECURRED R2026-08-25b** — same three probes, `admin:users.id/ADMIN/dark-1440`, 500 on a user id that was again an account *this run had created and soft-deleted minutes earlier* (the messaging-IDOR probe learner). Deleted-target is 2 for 2, but it is not sufficient: 5 sequential and **120 concurrent** GETs on that same id afterwards were all 200 (plus 10 correct 429s from the admin limiter). The other shared factor is sustained full-sweep load — ~9 min of continuous traffic against an API process that is also the Bull worker, which is O84's shape. Blocked on evidence no QA cycle can currently reach: the API logs its stack trace to the operator's terminal, so the sweep can record the 500 but never the throw. Capturing 5xx bodies during the sweep window is the cheapest way to stop adding tally marks. | 🟡 filed (recurring) | [#43](https://github.com/KAMRONBEK/talim-ai/issues/43) |
| F92 | S3 | US-OWNER · students · perf | **The student list jumps 38 px down ~0.4 s after load — CLS 0.289.** One shift event carries the whole score, and it has a single named cause: the seat-count pill at `students/page.tsx:304-308` is rendered only once `useBilling()` resolves (`{seats && …}`), with nothing reserving its height, so the join-code card (y 189→227), the copy button, the table container (429→467) and every row below drop together. A tutor reaching for "Kodni nusxalash" or a row's "Tiklash" as the page settles can hit the control that slides into its place. Deterministic: **0.289 on 3/3 cold loads**, and the sweep sees it on every variant (0.316 ru / 0.301 / 0.289 dark / 0.289 uz-light). Split out of **O98**, which keeps the *other*, genuinely threshold-straddling pages (learner dashboard, 0.14–0.32 by variant). Filed not fixed: reserving the space is a visual decision — an org with no billing row never shows the pill, so a permanent 38 px gap may read worse than the jump. Measured on the dev server (absolute CLS may be lower in a production build) but the mechanism and the 38 px box are build-independent. | 🟡 filed | [#44](https://github.com/KAMRONBEK/talim-ai/issues/44) |
| F93 | S4 | US-XCUT · messaging | **A completely blank message can be sent, and can never be removed.** `z.string().min(1).max(5000)` (`services/tenant/messages.ts:5-12`) is a *length* check, so `"   "` passes — all three write paths return **201**: `POST /tenant/messages`, `POST /tenant/messages/:id/respond`, `POST /learner/messages/:id/reply` (also with two `U+00A0`). The empty bubble lands in the other party's thread and lights their unread bell (owner unread-count → 2). The product clearly intends otherwise — every composer disables Send on `!draft.trim()` — so this is client-only validation the server does not mirror (self-consistency oracle). Same class as **F34** (org name) but a different schema; fixing one does not fix the other. What lifts it above "waive": there is **no `DELETE /tenant/messages/:id`** (404, route absent), so the artefact is permanent — the same append-only gap **O100** recorded for assessments. Fix is `z.string().trim().min(1)`; JS `trim()` covers the NBSP variant. Reproduced twice, second time from a fresh login. | 🟡 filed | [#45](https://github.com/KAMRONBEK/talim-ai/issues/45) |
| F94 | S3 | US-ADMIN · impersonation · **SECURITY — details withheld** | **An impersonated session is indistinguishable from a real one and cannot be exited.** Repo is public, so evidence lives in the advisory. Safe to say here: the feature itself is sound — impersonation tokens are refused on `/admin/*` (403), signature tampering and a `role: ADMIN` payload swap both 401, self/unknown/other-admin targets are refused (400/404/403), the TTL is exactly the 30 min the UI promises, `?token=` is stripped from the URL on landing, and the UI's claim "This action is recorded in the audit log" is **true** (every mint writes an IMPERSONATE row with admin + target email + role). What is missing is the marker: 0 banners, the word never appears in the rendered text, no exit control among any visible button, and the session survives a reload like any other login. The audit trail records that impersonation *started*, never what was done next — `impersonatorId` rides in the token but is read by nothing (deliberate, per the controller comment). Reproduced twice from fresh contexts. Filed at `low`: minting requires ADMIN and every guard holds, so the realistic failure is operator error, not attack. | 🔒 advisory | [GHSA-v8gx-9qfw-92f4](https://github.com/KAMRONBEK/talim-ai/security/advisories/GHSA-v8gx-9qfw-92f4) |
| F95 | S4 | US-IND · workspace · a11y | **The reading-workspace divider is mouse-only.** `role="separator"` with `aria-orientation="vertical"` and an Uzbek label promising it resizes the panels, but **no `tabindex`** (never focused in 45 then 50 Tab presses, while the other 35 focusables on the page all are), **no arrow-key handling** (`ArrowLeft/Right/Home/End` after a programmatic `.focus()` move it 0 px), and **no `aria-valuenow/min/max/controls`** — so ARIA announces a splitter that can be neither operated nor read. Mouse drag itself works and **persists across a real reload** (`content-workspace-split-v2`, 914 → 654 → 654), so only the keyboard path is missing. WCAG 2.2 **SC 2.1.1 (Level A)** failed for this control. Held at S4 rather than S3 because the workspace is fully usable at the default split — both panes render and the O'qish/Xulosa/Podkast/Test tabs are all keyboard-reachable; what is lost is the customisation, not the content. Same family as F90/#42 (game silent to AT) but a different component and criterion (2.1.1 vs 4.1.3). Reproduced twice, second time in a fresh context. | 🟡 filed | [#46](https://github.com/KAMRONBEK/talim-ai/issues/46) |
| F96 | S3 | US-IND-28 · flashcards · SRS | **A flashcard grade that failed to save took the card with it.** `onGrade` advanced the session queue synchronously, before the mutation resolved, and the error path only called `setError` — so a failed review POST showed *"Baho saqlanmadi. Qayta urinib ko'ring."* (not saved, try again) about a card the UI had already dropped, an instruction the product itself made impossible to follow. The card was skipped for the rest of the session and still counted as studied, while the server correctly kept it due (`reps 0`). Reproduced twice by fulfilling `POST /content/:id/flashcards/:cardId/review` with a 500 — second time from a fresh reload with a different grade button — both times 11 → 10 remaining. The advance is optimistic, so the error path now rolls it back (card returns to the head, studied count decremented); `again` already re-queues to the tail so the guard skips it. Verified: same injected 500 leaves queue 11 and counter 0/11 with the card on screen, and the next attempt grades it normally. | 🐛→✅ | `54e18ccf` |
| F97 | S3 | US-OWNER-21 / US-LEARNER-15 · GAME live | **Starting a live game never reaches a student who already has the dashboard open.** The tutor's own panel promises *"o'quvchilar boshqaruv panelida qo'shilish bannerini ko'radi"*, but `useLearnerAssessments` (`hooks/useAssessments.ts:335`) has neither a `refetchInterval` nor an event subscription — the only query in that file without one, while its siblings (`useQuestionBanks:61`, `useAssessmentLeaderboard:290`, `useLearnerLeaderboard:309`) are all explicitly SSE-primary with a safety-net poll. So a live game's *leaderboard* self-refreshes but the banner announcing the game does not. Banner absent at 5/15/30/50 s after go-live on an untouched page, present immediately after a real reload; server reported `isLive: true` throughout on both sides. Reproduced twice, second time from fresh state. Filed not fixed — choosing poll-vs-SSE here is a design call. | 🟡 filed | [#47](https://github.com/KAMRONBEK/talim-ai/issues/47) |
| F98 | S3 | US-OWNER-23 · CSV import | **A semicolon-delimited roster imports its own header row as a student.** `parseCsv` splits on `,` only, so a `;` file (Excel's default on ru/uz Windows — this product's market) parses line 1 as the single cell `"name;email"`, which misses `headerCols`, so `hasHeader` is false and the header falls through as data. Result: a student named `name;email`, plus one student per data row whose *whole line* is the name and whose email is never parsed. `HTTP 200`, every row `created`, no warning field. Same for comma files with non-English headers (`Ism,Familiya` → a student named `Ism`). A headerless-positional control proves the parser genuinely cannot tell "no header" from "a header I did not recognise". Reproduced twice from restored state. Filed not fixed: reject-vs-sniff is a product call. **Working, do not re-test:** BOM (`.trim()` strips U+FEFF — the "BOM breaks it" hypothesis was tested and *refuted*), CRLF, Cyrillic names, in-file duplicates → `skipped_duplicate`, formula-injection name stored raw on import (correct — escaping belongs on export, where it already is). | 🟡 filed | [#48](https://github.com/KAMRONBEK/talim-ai/issues/48) |
| F99 | S4 | US-OWNER · assign / students · a11y | **The student-search box announced as an unlabelled edit field.** Placeholder only, at both `assign-students-panel.tsx:72` and `students/page.tsx:341`; a placeholder is not an accessible name and disappears once the field has a value. Found by walking the assign page keyboard-only — the single unlabelled control among 28 focusables. Fixed by naming the field with the string it already renders, so no new i18n keys and all three locales covered by construction. Verified live: 0 unlabelled inputs on both pages. Everything else on that page passed — all 28 Tab-reachable, focus visible on each, all 5 student checkboxes label-associated, assigned row correctly checked+disabled. | 🐛→✅ | `256a1d5c` |
| F100 | S3 🔒 | US-LEARNER-14 · assessments · grading | **A forged ORDERING answer was worth 450% of its own question.** `orderingPairwiseCredit()` (`packages/types/grading.ts`) counts ordered pairs over the *submitted* list but divides by the pair count of the *key* — a numerator quadratic in submitted length over a fixed denominator, so any submission repeating key items exceeds 1 without limit. Measured through the real submit endpoint on a `strictScoring` assessment, question worth 1: `["1","1","2","2","3","3","4","4"]` → `creditFraction` **4.0**, `pointsEarned` **4.0**, `maxPoints` 1; `["1"×8,"2","3","4"]` → **4.5**. `services/assessment/learner.ts` computes `questionPointsEarned = creditFraction * qPoints` with no clamp and persists it as `AssessmentAttempt.pointsEarned`, so weighted points exceed the maximum. The player cannot produce a duplicate-bearing ordering; an HTTP client can. Reproduced identically on a second, freshly created assessment. **Scoped honestly:** every sibling grader was checked and is bounded (MULTIPLE_SELECT clamps explicitly; MATCHING/DRAG_DROP/FILL_BLANK/DROPDOWN_CLOZE count hits over a key-bounded loop), and the Elo-KT mastery model already clamped its own input (`sectionMastery.service.ts:112`) — `pointsEarned` was the whole exposure. Fixed by deduplicating submitted items (an ordering places each item once, so a repeat carries no ordering information) then clamping to 0..1. Verified before/after: forged rows 4.0/4.5 → 1.0/1.0 with `pointsEarned == maxPoints`, legitimate rows byte-identical (key 1.0, reversed 0 with the −0.5 penalty, one adjacent swap 0.8333), 7-type truth-table unchanged. Abuse-shaped → draft advisory, never a public issue. | 🐛→✅ | `07475d9f` · [GHSA-mmv8](https://github.com/KAMRONBEK/talim-ai/security/advisories/GHSA-mmv8-jv85-97jg) |
| F101 | S3 | US-LEARNER-14 · EC7 · assessments | **An ORDERING question you never touched is submitted as a real answer.** Submitting the learner form with every field untouched persists six empty answers and one full one: `ORDERING: ["3","4","1","2"]` while MULTIPLE_SELECT is `[]`, MATCHING `["","",""]`, NUMERIC `""`. That answer is worth **0.333 creditFraction** against a genuinely blank answer's 0. The starting order is shuffled **server-side** and is stable across real reloads (`GET /learner/assessments` serves the same `options`), so it is not a per-load lottery — every learner who skips the question gets the same free credit, sized by how far the shuffle landed from the key. Reproduced from the browser (persisted attempt) and twice more through the API on fresh assessments. Filed not fixed: `question-inputs.tsx:120` says *"The starting order is a valid ORDERING answer"*, so whether an untouched order counts is a product decision. What is hard to defend either way is that a skip and a deliberate answer look identical, the untouched question earns partial marks, and the amount is set by the shuffle. Scoring impact needs `strictScoring` (default off); the UI half applies always. Pinned from the long-standing US-LEARNER-14·EC7 candidate, and downgraded from its speculative S2 by the measurement. | 🟡 filed | [#49](https://github.com/KAMRONBEK/talim-ai/issues/49) |
| F102 | S3 | US-IND · podcast | **One podcast episode shows two different lengths on the same screen.** The episode card says **1:17**, the player under it says **1:31**, and the file is 91.75 s. The card renders `durationSec`, which `generatePodcast.job.ts:135` computes as `Math.max(60, Math.round((wordCount / 150) * 60))` — a 150-words-per-minute English-prose rate applied to Uzbek narrated by English-trained OpenAI voices, so it is consistently short rather than noisy. Reproduced on a second fresh navigation. Promotes **O82** (confirmed R20 at 93 s vs 103 s) and **corrects its root cause**, which was recorded as an mp3 byte-length estimate — that constant builds the segment timeline, not `durationSec`; the wrong cause is what justified deferring it to PLANS as structural. **Checked and healthy, do not re-test:** transcript click-to-seek (all 12 lines seek monotonically 0 → 87.19 s inside the 91.75 s file, none past the end) *despite* a stored segment timeline of 305 840 ms — `TTS_BYTES_PER_MS = 6` is Azure's 48 kbit/s figure and the provider moved to OpenAI on 2026-08-23, but the client normalizes against the real duration so the stale constant cancels out; and playback position persists across a real reload. **Oracle:** the episode script scores 11/11 atomic claims against `fixtures/uz-math-facts.md` with zero trap answers. | 🟡 filed | [#50](https://github.com/KAMRONBEK/talim-ai/issues/50) |

| F103 | S3 | US-OWNER · assessments · perf | **The question-bank card jumps 474 px down on `/tenant/assessments`.** The live-game section (`assessments/page.tsx:548`, `{assessments.some((a) => a.mode === 'GAME') && …}`) does not exist in the DOM until `GET /tenant/assessments` resolves, and it mounts *above* the rest of the page with nothing reserving its height. Measured on a **production build**: CLS **0.302 / 0.312 / 0.235** on 3 cold loads, one dominant event whose attributed source moves the question-bank `SECTION` from **y 319 → 793**. The arithmetic closes on the settled page — the live section is **442 px** tall at y 330 and the bank sits at y 804, a 474 px gap. Conditional on data (a tutor with no GAME assessment never sees it), which is why it can read as intermittent. Same shape as **F92/#44** but a different component and a 12× larger box, so filed separately. Filed not fixed: reserving 442 px permanently would leave a hole for tutors with no GAME assessment, so the choice between a loading skeleton and reordering the page is a visual decision. | 🟡 filed | [#51](https://github.com/KAMRONBEK/talim-ai/issues/51) |

| F104 | S3 | US-IND · slides · EC7 | **AI slide decks render raw LaTeX.** A student reads `$c^2 = a^2 + b^2$`, `$a \neq 0$` and `$D = b^2 - 4ac$` as literal text — **25 raw `$…$` spans across 3 of 5 slides, 0 `.katex` nodes**, measured on the live page and reproduced from a cold navigation. Not the model going off-script: `deck-prompt.ts:64` *instructs* it to write `$...$`, and `DeckMarkdown.tsx:18-23` is already wired with `remarkMath` + `rehypeKatex`. The gap is that **`DeckMarkdown` has exactly two call sites** (`Slide.tsx:149` TwoColumn, `:358` Callout) — both fields literally named `markdown` — while `definition`, `example`, `recap.points`, `bullets.text/notes`, `concept.body`, `quickCheck.question/answerExplanation` and 6 more layouts interpolate plain text. Intermittent **by generation, not by page**: an earlier deck from the same PDF used Unicode superscripts and looked fine, so which deck a student gets is a coin flip. Filed not fixed — routing 13 more fields through a markdown renderer changes rendering semantics deck-wide (a stray `*` becomes emphasis), which is a visual-design call. **Oracle: the deck's facts are correct** — 10/10 atomic claims against `fixtures/uz-math-facts.md`, zero trap answers — this is purely rendering. Likely also hits AI video, which builds a deck through the same service (`slides.service.ts:297`); not verified. | 🟡 filed | [#52](https://github.com/KAMRONBEK/talim-ai/issues/52) |

| F105 | S4 | US-IND · chat · EC7 | **Every AI-tutor message is stamped "Hozir" (now) forever.** `ChatMessage.tsx:65` renders a constant — `{isUser ? t('you') : t('tutor')} · {t('now')}` — and `ChatMessageProps` (`:8-14`) has no timestamp field at all, so a four-turn conversation sent over 100 seconds, then reloaded, shows all eight messages as "Hozir". Both halves of the fix already exist and are unused: `GET /chat/content/[id]/messages` returns `createdAt` per message (verified), and `lib/format-relative-time.ts:49` exports `formatRelativeTime(date, locale)`. Filed not fixed — threading `createdAt` through the props means deciding what a still-**streaming** message shows and handling SSR/CSR hydration drift on a relative time. Nothing is lost: messages are correct, ordered and persistent; the label is wrong. | 🟡 filed | [#53](https://github.com/KAMRONBEK/talim-ai/issues/53) |
| F106 | S3 | US-IND · chat · reliability | **Reloading while the tutor is answering loses the answer on screen.** Reload mid-stream and the conversation keeps your question with nothing under it — no answer, no spinner, **0** `[role=alert]`, no retry — while the server finishes normally and stores the assistant row (verified in the DB at `00:15:09.113Z` and `00:16:39.416Z` for the two repros). `useChat.ts:6-17` fetches the message list **once on mount** with no `refetchInterval`, no invalidation and no subscription, so a reload landing between "user row written" and "assistant row written" renders against a cache nothing will ever refresh. Recovery is not "reload twice" — repro 2's second reload landed a second before the row was written and also showed nothing; recovery is reloading at some point *after* the answer happens to land. The user's `TUTOR_MESSAGE` quota is charged before the stream starts, so they pay for an answer they cannot see. S3 not S2 because nothing is actually lost. The product already has the mechanism to fix it — the SSE `/events` channel drives live updates elsewhere. | 🟡 filed | [#54](https://github.com/KAMRONBEK/talim-ai/issues/54) |

| F107 | S3 | US-LEARNER-14 · assessments · a11y | **A DRAG_DROP question cannot be answered with a keyboard.** The chips are real `<button>`s with `aria-pressed`, so a keyboard user *picks an item up* — but the target buckets are `<div>`s carrying only `onClick`/`onDrop` (`learner/assessments/page.tsx:665-681`): **no `tabIndex`, no `role`, no `onKeyDown`**, verified live as `tabindex: null, role: null` on all four buckets both before and after pick-up. Measured three ways: mouse click chip→bucket moves the counter `0/3 → 1/3`; `Enter` on a chip then `Tab`×6 walks straight past both buckets into the next question's `<select>`s; and a full keyboard-only pass over the form submits with **`0/3 joylashtirildi`**, scoring **2 of 7 (29%)** and spending the attempt (`Urinishlar: 2/5`, persisted across a real reload). The page advertises the alternative it doesn't finish — *"elementni, so'ng guruhni bosing"* — so the intent is clearly there; the bucket just never got the chip's treatment. **Touch is fine** (tap-to-pick/tap-to-place covers what HTML5 drag can't). WCAG 2.2 **2.1.1 Keyboard (A)**, and unlike F93/#46 there is no other way to earn the marks. Filed not fixed: giving the bucket `role="button"` + `tabIndex` + a key handler is a real interaction-design change to a graded control. **Checked and healthy, do not re-test:** ORDERING has Yuqoriga/Pastga buttons, DROPDOWN_CLOZE + MATCHING use native selects, MULTIPLE_SELECT checkboxes are `<label>`-wrapped, and 22 of the card's 24 focusables are reachable with a visible focus ring. | 🟡 filed | [#55](https://github.com/KAMRONBEK/talim-ai/issues/55) |

| F108 | S3 | US-LEARNER · onboarding · AUTH | **A student who signs themselves up with a class code is told their teacher created the account.** The learner welcome banner reads *"O'qituvchingiz bu hisobni yaratdi. Xavfsizlik uchun vaqtinchalik parolni Sozlamalarda o'zgartiring"* — but a self-registered joiner chose their own password seconds earlier on `/register`, and `POST /auth/register` returned `mustChangePassword: false` for them. `student-welcome-banner.tsx:19` ORs the server flag with a **per-device localStorage default** (`onboarding.ts:9` returns `true` whenever the key is absent), so `mustChangePassword` never actually gates the banner and dismissal does not travel: cleared storage → real form login → the banner is back. Reproduced twice, the second time from fully cleared storage. Filed not fixed — "hide it for self-joiners" vs "give self-joiners their own copy" is a product call, and the copy needs rewriting either way. Oracle: **Claims** (the product asserts something untrue about how the account was made) + product self-consistency. | 🟡 filed | [#56](https://github.com/KAMRONBEK/talim-ai/issues/56) |

| F109 | S3 | US-IND · video · perf | **An AI video gets longer while you watch it** — the player says **1:23** on slide 1 and **1:41** on slide 5, while the stored `durationSec` is **79** (1:19). `NarratedVideoPlayer.tsx:74` seeds the per-segment `durations[]` from the *server's estimates* and sums them into `totalDur` (`:94-98`); each entry is corrected to the real audio length only in `<audio onLoadedMetadata>` (`:362-372`), which fires for a segment **only once the viewer reaches it**. The estimates run systematically short (segment 4: 16 s stored vs **19.42 s** real; segment 5: 16 vs **24.60**), so the total only ever grows. Not just a label: `totalDur` also drives the seek fraction (`:332`), the chapter-tick offsets (`:426`) and `aria-valuemax` (`:401`), so the ticks slide and a drag-to-seek maps differently before and after. Measured 1:23 → 1:32 → 1:41 in one sitting, reproduced from a fresh load. Same family as **F102/#50** (podcast card vs player) but a different mechanism — there two static numbers disagree, here one number is unstable within a single sitting. Fix belongs upstream: the job synthesises the audio and could measure it instead of estimating. **Oracle: the narration is correct** — 9/9 atomic claims against `fixtures/uz-math-facts.md`, zero traps, and unlike F104 the script uses Unicode superscripts so no raw LaTeX reaches a student. | 🟡 filed | [#57](https://github.com/KAMRONBEK/talim-ai/issues/57) |

| F110 | S3 | US-OWNER · students | **Resetting an email-less kid's password shows only the password — not the username they sign in with.** Promotes **O108**, whose UI consequence had been read from source but never captured; it is captured now (`docs/qa/screenshots/c6-reset-panel-no-username.png`). The panel says *"give this information to the student (shown once)"* and renders `Parol: …` alone, while **Nusxalash** copies the password by itself. Cause re-verified live in one session: `GET /tenant/students` returns `email: null, username: "qakid5"`, `POST /tenant/students/:id/reset-password` returns `email: "qakid5@students.talim.local", username: null` for the same row seconds later, and `POST /tenant/students` (create) returns the **list's** form (`username: "qakid6"`). The UI trusts the mutation response (`students/page.tsx:545,634`) and hides the username line when falsy (`:715`), so the *same* panel shows the username on create and drops it on reset. S3 because the tutor can read the username off the table row. Clean fix is at the API — make reset (and `PATCH`) return the same presentation form as list/create. **Method note that unblocked it:** the panel is **not** a `[role=dialog]` node, which is why two earlier attempts to capture it timed out. | 🟡 filed | [#58](https://github.com/KAMRONBEK/talim-ai/issues/58) |

| F111 | S3 | US-IND · slides · EC7 | **AI slide decks silently drop slides.** Promotes **O105** and settles its open question. The newest generation of `uz-math.pdf` is a **4-slide deck** whose cover promises *"Pifagor teoremasi, kvadrat tenglamalar va to'plamlar"* and whose recap summarises quadratics and sets, with **no body slide for either**. Decided by **id arithmetic**, not by reading the log: the model names its own slides sequentially (`deck-prompt.ts:131` example `{"id":"s1"…}`, `:145` *"Give every slide a unique id"*), so a model that wrote four slides would name them `s1,s2,s3,s4` — `s1,s2,s5,s6` means six were drafted and two removed with the survivors keeping their numbers. That is precisely `coerceDeck`'s salvage path (`slides.service.ts:214-228`), whose own comment predicts the symptom: *"A dropped slide is content the learner silently never sees, and the deck's own title/recap may still refer to it."* Five generations of this content, every one non-contiguous (`s1,s2,s3,s5,s6` ×3, then `s1,s2,s5,s6` ×2 on two fresh in-cycle regenerations 90 s apart with entirely different wording); the control on a different source is `s1,s2,s3,s4,s5,s6`. `estimatedMinutes` is recomputed from the survivors, so nothing downstream disagrees. **Structural note:** R2026-08-25e's instrumentation (`console.warn`) is **unreadable from a QA cycle** — the API logs to the dev server's stdout and no log file exists in the repo; to be diagnosable it needs to be a field on the deck row or a counter. | 🟡 filed | [#59](https://github.com/KAMRONBEK/talim-ai/issues/59) |

| F112 | S2 | US-ADMIN · users · subscriptions | **One user's long name makes the admin Users and Subscriptions pages unusable — and it is filed as a draft advisory, not an issue, because anyone can cause it.** `POST /auth/register` is public and puts no maximum on `name` (`auth.controller.ts:19`, `z.string().min(1).optional()`), so a 600-character unbroken token lands in the admin tables, which are auto-layout `<table>`s inside `overflow-hidden` wrappers with no truncation. On `/users` the table measures **7270px inside a 1152px wrapper**: Password, Role, Plan, Status, Content and **Actions** (impersonate / edit / reset password) are clipped away, and `document.documentElement.scrollWidth === window.innerWidth`, so there is **no scrollbar to reach them** — the operator cannot administer any user while such a row is on the page. `/subscriptions` is the same at ~5820px, hiding the Edit column. **Control that isolates the cause:** searching to exclude that one row collapses the table to **1150px**, it fits, and the Actions buttons return — one row disables the whole page. Reproduced twice, the second from a fully fresh browser and login. **Every existing probe stayed green**, `no-horizontal-overflow` included, because `overflow-hidden` is exactly what suppresses page-level overflow — so a `no-clipped-content` probe was added to `qa-sweep.mjs` this cycle and validated on 59 cells (fires on these two, silent on the other 12 admin cells and all 45 owner/learner web cells). Same root as **#34** and **O110** on a different field; the client half must be fixed too, since bounding the input does not repair existing rows. | 🔒 advisory | [GHSA-jvw8-v32q-m53h](https://github.com/KAMRONBEK/talim-ai/security/advisories/GHSA-jvw8-v32q-m53h) |
| F113 | S3 | US-ADMIN · users · usage | **"Usage vs limits (this month)" and "API cost (MTD)" both show today only.** `getUsageVsLimits` runs on `dayRange()` — midnight local to now (`subscription/shared.ts:89-93`, used at `subscription/user.ts:204`) — while the admin user page labels the panel *this month* (`users/[id]/page.tsx:608`) and the cost tile *MTD* (`:647`). Measured on `qa-individual` at 2026-08-25 (UTC+5): the window starts `2026-08-24T19:00:00Z`, which is **local midnight today**, not local month start (`2026-07-31T19:00Z`); and the page shows **API cost (30d) $0.167118** beside **API cost (MTD) $0.082394** for an account whose activity is entirely inside this month — the "MTD" figure is half the 30-day one. The limits printed next to the counters are themselves the per-day fields (`maxUploadsPerDay`…), which is the giveaway: "Uploads 0 / 3" reads as three a month. Numbers are right, labels are wrong; relabel to *today* rather than widen the window, since these are the same values the quota middleware enforces. Oracle: product self-consistency. | 🟡 filed | [#60](https://github.com/KAMRONBEK/talim-ai/issues/60) |
| F114 | S3 | US-ADMIN · subscriptions · audit | **Changing a subscription's period end is audited as a no-op.** `PATCH /admin/users/:id/subscription` logs only `fromPlan/toPlan/fromStatus/toStatus` (`admin/users.controller.ts:380-391`); `currentPeriodEnd` is accepted by the same request and written at `subscription/user.ts:97-99` but never reaches the log. Observed live: an edit that moved a period end from `2026-09-30` to **`2020-01-01`** produced `{"toPlan":"INDIVIDUAL_PRO","fromPlan":"INDIVIDUAL_PRO","toStatus":"ACTIVE","fromStatus":"ACTIVE"}` — every recorded field unchanged, so the audit trail says the operator opened the drawer and saved nothing. Matters more than it looks: activation and billing here are **manual**, so the audit log *is* the billing record. The organization equivalent (`admin/tenants.controller.ts:161-167`) logs `metadata: body` and **does** capture the field, so the two admin paths disagree and the individual one is the lossy side. Oracle: product self-consistency. | 🟡 filed | [#61](https://github.com/KAMRONBEK/talim-ai/issues/61) |
| F115 | S2 | US-IND · content · chat | **Selecting a region of a PDF sends the AI tutor text from outside the selection.** pdf.js positions text-layer spans in CSS as `calc(var(--total-scale-factor) * Npx)`; `pdf-text-layer.css:15` declares `--scale-factor: 1` on `.pdf-page` as a placeholder and `PdfViewer.tsx:280-293` computes the real scale, renders the canvas *and* the `TextLayer` at it, and never writes it back — so the text layer stays at the PDF's intrinsic **612px** while the canvas is fitted to its container (**844px at 1440**, **382px at 390**), i.e. the invisible selectable text sits somewhere other than the glyphs at every window size except one. That is what the marquee reads: `extractTextFromTextLayerMarquee` (`pdf-area-selection.ts:90-101`) intersects the drawn rectangle against those span rects. **A/B on the identical top-left 400×300 drag of an 844px page:** *before* — the whole first paragraph, including `tomonlari orasidagi`, `deydi: gipotenuzaning kvadrati` and `ya'ni a^2 + b^2 = c^2`, all drawn to the **right** of x=400, plus lines below y=300; *after* — each line clipped at the box edge. The `extractTextInDomRect` fallback uses the correct viewport transform but only fires when the DOM path returns **too little** text, so the over-inclusive desktop case never reaches it and fails silently with plausible-looking output. Surfaced by the `no-clipped-content` probe added last cycle, which saw the 612px layer clipped inside a 382px page. Oracle: product self-consistency. **Fixed** (`1e7b7dff`) — layer width now equals canvas width at 390 (scale 0.624) and 1440 (scale 1.379). | ✅ fixed | [#62](https://github.com/KAMRONBEK/talim-ai/issues/62) |
| F116 | S3 | US-IND · content · flashcards | **On a 360px phone the Flashcards tab is off the screen edge with nothing to scroll it into view.** `content-stage.tsx:304-338` is a flex header whose five-tab mode toggle is `shrink-0` with `overflow-x: visible`, so once the tabs need more room than is left it overflows and the `overflow-hidden` ancestor cuts it. At 390 "Kartalar" is truncated by 32px; at **360** — the common Android width — it spans **x 349–422 on a 360px screen**: an 11px sliver, centre off the edge, `elementFromPoint` at that centre not returning the button. The row never becomes a scroll container (`scrollWidth − clientWidth === 0`) and `document.documentElement.scrollWidth === window.innerWidth`, so nothing scrolls; a scan of every `<a>` on the page finds **no other link to flashcards**. (`ru`/`en` show only two tabs on the same material — that is **#9**, locale-scoped artifacts, not this.) **Fixed** (`1ead136f`) — the group is `min-w-0 overflow-x-auto` with `shrink-0` tabs; at 360 clicking Kartalar lands on `/flashcards`, at 1440 max scroll is 0 so the desktop is unchanged. | ✅ fixed | [#63](https://github.com/KAMRONBEK/talim-ai/issues/63) |

| F117 | S3 | US-ALL · input validation | **Typing a NUL byte into any text field returns a 500 instead of a validation error.** `U+0000` is legal inside a JSON string and passes every zod check we apply — `z.string().trim().min(1).max(5000)` sees a perfectly good 12-character value (`tenant/messages.ts:8`, `:12`) — but PostgreSQL rejects the byte outright in a `text` column, Prisma throws, and nothing catches it, so `error.middleware.ts` returns a generic 500. Not one endpoint: confirmed on `POST /auth/register` (**unauthenticated**), `POST /tenant/students`, `POST /tenant/messages` and `POST /learner/messages/:id/reply`. Reproduced 2/2 on fresh logins; **control** — the identical payload with the byte removed returns 201 every time. Nothing is written, no stack trace escapes, and `/health` is 200 immediately after, so the defect is the status code and the missing message, not data loss. Filed rather than fixed: patching the two message schemas would leave register/students and every future field behind, so the durable shape is one shared zod string helper at the validation layer. Oracle: standards (HTTP 400 vs 500). | R2026-08-25i | [#64](https://github.com/KAMRONBEK/talim-ai/issues/64) |
| F118 | S2 | US-TENANT · students · CSV | **Exporting your class in Uzbek and importing it back creates nobody — every row says "Name is required".** The export writes a **localized** header (`students/page.tsx:151` → uz `Ism,Email,Holat`, ru `Имя,Email,Статус`, en `Name,Email,Status`) while `parseCsv` matches a hardcoded English list (`tenant/students.ts:249-257`). `Ism,Email,Holat` trips the worst of both branches: `'email'` **is** in the list so `hasHeader` becomes true and the header is correctly skipped, but `first.indexOf('name')` is **-1** because the column is called `Ism`, so `pick(-1)` is `undefined` and every row hits the name guard at `:345`. Needs a header containing *one* recognised column but not `name` — exactly what our own export produces in every non-English locale. Proven through the real UI: Eksport → paste the captured bytes into the real import dialog → **"Yaratildi 0 · Qayta faollashtirildi 0 · O'tkazib yuborildi 0 · O'rin cheklovi 0 · Xatolar 2"**, both rows *Name is required*, on rows that visibly have a name in column 1. uz and ru broken, **en works**, which is why it went unseen. Filed not fixed — the repair is a contract decision (canonical export headers vs localized-label aliases vs positional fallback), not a patch. Oracle: product self-consistency. | R2026-08-25i | [#65](https://github.com/KAMRONBEK/talim-ai/issues/65) |
| F119 | S3 | US-TENANT · students · CSV | **A name with a line break in it imports as two students, and one is named after their own email.** `tenant/students.ts:215` splits the file into lines *before* any quote handling (`csv.split(/\r?\n/)`), so `parseLine` — which tracks quotes correctly — can never see that a quoted field continued onto the next line. `"Ali\nValiev C9",qa.csvNl.C9@example.com` (legal RFC 4180, and what Excel writes whenever a cell holds a newline) yields **two `created` rows**: `Ali`, provisioned email-less with a generated username, and `Valiev C9,qa.csvNl.C9@example.com` — the trailing `"` opens a *new* quoted run that swallows the comma, gluing the email into the name. Both consume a seat; the real student is never created with their email; the report says `created` twice and nothing warns the tutor. **Controls** isolate it to the line split: quoted comma, CRLF, and UTF-8 BOM all parse correctly (`String.trim` strips `U+FEFF`). Filed not fixed — the repair is a single-pass rewrite of `parseCsv` that carries quote state across newlines, and it wants tests of its own. Oracle: standards (RFC 4180). | R2026-08-25i | [#66](https://github.com/KAMRONBEK/talim-ai/issues/66) |

**High-probability finding candidates flagged during this expansion** (each needs the §E bundle before it earns an F#):
- Impersonated session is unrestricted + non-revocable after exit (US-ADMIN-08b·EC16) — **S1** hypothesis.
- FLAGGED media not actually hidden from learners (US-ADMIN-11·EC3) — **S2** or product-gap.
- B2C/tenant flashcards POST lacks `enforceQuota` → free generation (US-IND-28·EC17) — **S2**.
- `shared.ts:168` "informational only" comment contradicts `learner.ts` 403 due-date enforcement (US-OWNER-20) — pin, then S3 doc fix.
- ~~ORDERING untouched-initial-order as a free point (US-LEARNER-14·EC7) — **S2** candidate, pin first.~~ **Pinned R2026-08-25d → F101 ([#49](https://github.com/KAMRONBEK/talim-ai/issues/49))**, and the measurement lowered it to **S3**: it is a free 0.333 credit, not a free point, and scoring impact needs `strictScoring` (default off).
- Forged `responseMs` inflates GAME leaderboard (US-LEARNER-16·EC4) — ties existing F39.
- Analytics empty-DB divide-by-zero + 429 on rapid refresh (US-ADMIN-10·EC2/EC5) — **S2**.

## B.2 — Observations ledger (`O<n>`) — NEW section, place after the Findings ledger

> **Observations (`O<n>`)** capture non-defect signals: enhancements, preferences, one-off
> non-reproducible oddities, and low-confidence Uzbek-fluency doubts for morning human review.
> They are **not** findings (no F#) and never block a run close. **Mandatory re-triage each run:**
> an `O<n>` that becomes reproducible with a named oracle + evidence bundle is promoted to an `F<n>`
> (this closes the F63 "dismissed as artifact → real bug" failure). Enhancements stay observations.

| O# | Kind | Story · EC | Note | Run seen | Re-triage status |
| --- | --- | --- | --- | --- | --- |
| O80 | uz-fluency-doubt | US-XCUT-01 | Generated content + app-wide uz UI strings use ASCII apostrophe `U+0027` in `o'`/`g'` where Uzbek orthography wants `U+02BB` (`oʻ`/`gʻ`). Mirrors source PDF + `messages/uz.json` convention → product-wide decision, morning human review. | R18 | open |
| O81 | security-hardening | US-ADMIN-08b | Impersonation tokens are **not single-use** — stateless 30-min JWT, no server-side nonce, so a token replays for its whole window (verified: 2nd identical request → 200). Deliberate stateless tradeoff; true single-use needs jti tracking. | R18 | open |
| O82 | media-metadata | US-IND · podcast | Podcast episode-row duration under-estimates the real audio. **R20 confirmed with a named oracle:** episode 1 stored `durationSec`=93s ("1:33") vs the loaded `<audio>.duration`=103s ("1:43", readyState=4 = ground truth), ~11% short; player time is correct, only the list label is off. Root cause = mp3 byte-length≈ms estimate at synthesis (drifts on VBR/ID3/padding). Fix is structural (probe decoded duration post-synthesis) → **deferred to `docs/PLANS.md`**. | R19 | R2026-08-25d: **promoted to F102** ([#50](https://github.com/KAMRONBEK/talim-ai/issues/50)) — re-measured on a second episode (77 s stored vs 91.75 s real, 16% short) and **the stated cause above is wrong**: `durationSec` is not the mp3 byte-length estimate but a 150-words-per-minute *word-count* guess (`generatePodcast.job.ts:135`); the byte-length constant builds the segment timeline instead. The wrong cause is what justified the structural deferral, so the deferral is retired with it. |
| O83 | copy | US-IND · podcast | Podcast transcript click-to-seek hint said "Videoning" on an audio podcast. | R19 | R20: **promoted to F81 (fixed)** |
| O84 | flaky-suspect | US-LEARNER · billing | `GET /billing/me` returned a one-off **500** on a fresh learner's first dashboard load, then self-healed (browser retry 200; 8/8 curl 200; **4/4 fresh-account first-calls 200**). Non-reproducible → transient (likely API-process saturated finishing a podcast TTS Bull job; API==worker). Not elevated. | R19 | open (transient-confirmed) |
| O85 | security-UX | US-ADMIN-08b | Impersonated learner session shows **no "you are impersonating" banner** — an admin acting as a user has no in-app indicator (session correct + audited). A persistent banner + one-click exit would reduce acting-under-identity-unaware risk. Enhancement. | R19 | open |
| O86 | UX | US-LEARNER · deactivation | A learner deactivated **mid-session** (valid JWT) sees the assigned list silently empty with **no "your account was deactivated" message** — only the login path (F16) explains it. | R19 | R2026-08-25a: **promoted to F86** (#41) — bundled with an oracle and the 401-only interceptor pinned as the cause |
| O87 | perf | US-IND · practice | Structured-type practice generation (DROPDOWN_CLOZE+MATCHING+ORDERING ×10, whole-material) took **~130s** — much slower than SHORT_ANSWER (~35s); SSE kept the UI honest (no stuck spinner) but it's a long wait. | R19 | open |
| O88 | UX | US-OWNER · material-media | A podcast episode with a missing `audioPath` renders as "Tayyorlanmoqda" (Preparing) — indistinguishable from an in-flight generation; there is **no per-episode FAILED/error affordance** (episodes have no status column; readiness derives from `audioPath`). Only bites if one episode's TTS drops audio while the podcast row stays READY. State was induced artificially (nulled audioPath) → speculative. | R20 | open (low-confidence) |
| O89 | docs | US-XCUT · i18n | `apps/web/CLAUDE.md` §2 warns `game-quiz-player.tsx`/`leaderboard-table.tsx` "still contain hardcoded English strings" — **stale**: both are now `useTranslations`-driven (68× / 4×) and render fully localized (uz verified C3; ru/en chrome clean C5). Doc nit only. | R20 | open (docs) |
| O104 | data-quality | US-OWNER · assessment builder | The manual question editor accepts a MULTIPLE_CHOICE with **two identical options** — `options: ["alpha","alpha"]`, `acceptableAnswers: ["alpha"]`, persisted with status APPROVED and rendered to the learner as two indistinguishable radios. `isAnswerableMultipleChoice` guards the opposite failure (no option matches the key, guaranteeing 0%) and is satisfied here, so nothing catches it. Harmless in grading — both normalize to the same string, so either pick is correct — which is why it stays an observation rather than an F. The same editor **correctly** refuses whitespace-only options (submit stays disabled). | R2026-08-25d | open |
| O105 | ai-output-consistency | US-IND · slides | **Both slide decks generated from `uz-math.pdf` name a topic no slide teaches.** The deck title ("…kvadrat tenglamalar **va to'plamlar**"), the recap points (`A = {1,2,3}` va `B = {2,3,4}` → kesishma `{2,3}`) and `sourceRefs` (`3-bob`) all reference set theory, but there is **no body slide about sets** in either deck. Both are also missing exactly slide **`s4`** — ids run `s1,s2,s3,s5,s6` on two independent generations 40 minutes apart, with completely different wording and layouts otherwise (gen 1 concept/process/Unicode superscripts, gen 2 definition/bullets/LaTeX). Two candidate causes and **no way to separate them from a QA cycle**: either the model drafts 6 and omits one, or `coerceDeck`'s salvage path (`slides.service.ts:214-219`) discarded the sets slide for failing `slideSchema`. The salvage path recorded **nothing** — no log, no counter, no field on the deck — which is the same blindness that left F91 unresolved through four sightings, so it was instrumented this cycle (`console.warn` naming the dropped slide's id, layout and zod issues). Kept an O rather than an F because the harm is unproven (a recap citing untaught material is a coherence defect; a model choosing 5 topics over 6 is not) and the cause becomes decidable on the next generation. Oracle: product self-consistency. **Control, same cycle:** a third deck generated from a *different* source (the tenant's YouTube material, `cmq2czlkb…`) came back **`s1,s2,s3,s4,s5,s6` — six slides, no gap** — so the drop is not happening on every generation. That cuts against a systematic server-side drop and towards something specific to `uz-math.pdf` (its sets section is the shortest, and the slide covering it is the one absent both times). Narrowed, not closed. | R2026-08-25e | **RESOLVED R2026-08-25f → promoted to F111 / [#59](https://github.com/KAMRONBEK/talim-ai/issues/59)**. It is the salvage path, decided by id arithmetic without needing the log. |
| O106 | a11y | US-IND · slides · quickCheck | **A slide-deck quick-check never tells you whether you were right.** `Slide.tsx:388-423` tracks a single `revealed` boolean and does not record which option was picked, so answering "8" to *"katetlari 6 va 8 → gipotenuza?"* dims every wrong option to `opacity-50`, rings the correct one in `border-emerald-500` with a `<Check/>`, and leaves the learner's own choice styled identically to the two they never touched. Correct/incorrect is carried by **colour, opacity and an untitled icon** — no `aria-pressed`, no live region announcing the reveal, no text. Mitigated, which is why it is an O and not an F: `answerExplanation` is plain readable text and states the answer outright ("…demak gipotenuza $c = 10$"), so a screen-reader user loses immediacy, not information. Reveal-on-any-click is clearly deliberate, so scoring the pick would be a product change. Same class as **O102** and **F90/#42**. | R2026-08-25e | open (design call) |
| O107 | a11y | US-LEARNER-14 · assessments · a11y | **The NUMERIC and FILL_BLANK answer fields are named by their placeholder, not their question.** Both inputs in the structured player carry only `placeholder` — *"Raqam bilan javob bering"* / *"Javobingizni yozing"* — with **no `aria-label`, no `aria-labelledby`, no wrapping `<label>` and no `label[for]`** (`learner/assessments/page.tsx:717-726`). Per ACCNAME the placeholder *is* a fallback accessible name, so this is not the unlabelled-field failure F99 fixed — the field announces. What it announces is the hint ("answer with a number"), never the question ("x² − 5x + 6 = 0 tenglamaning katta ildizi nechaga teng?"), and a placeholder also disappears the moment the learner types. In a 7-question form a screen-reader user tabbing between fields cannot tell which question they are on without leaving forms mode. Kept an O because the fix is a choice — `aria-labelledby` pointing at the question needs the question node to carry an id, and whether the *number*, the *text* or both should be announced is a design call. Same family as O102, O106 and F99. | R2026-08-25e | open (design call) |
| O108 | data-consistency | US-OWNER · students | **Single-student mutation responses return the raw row; the list returns the presentation form.** For the same email-less kid, `GET /tenant/students` reports `email: null, username: "qakid5"` while `POST /tenant/students/:id/reset-password` and `PATCH /tenant/students/:id` both report `email: "qakid5@students.talim.local", username: null` — verified on the same student, minutes apart, on three endpoints. Create is on the list's side (`username: "qakid5"`). The consequence is in `tenant/students/page.tsx:545,634`, which builds the post-reset credentials dialog from the mutation response: `setCredentials({ username: data.student.username, password: data.temporaryPassword })`, and the dialog renders the username only `{credentials.username && …}` (`:715`) while the copy button falls back to the password alone (`:729-731`). So the dialog a tutor gets after resetting an email-less kid's password should show the new password with **no username** — the one field that child needs to sign in, since they have no real email. Logged as an O, not an F, with the boundary drawn honestly: the API inconsistency is **verified three times**; the dialog consequence is **read from source and was not captured in the browser** (two attempts to open it in the budget available failed to render `[role=dialog]`). Pin or dissolve next cycle — it is a two-minute check with the dialog open. | R2026-08-25e | **PINNED R2026-08-25f → promoted to F110 / [#58](https://github.com/KAMRONBEK/talim-ai/issues/58)**. The panel was captured, and it shows the password alone exactly as predicted. The two earlier attempts failed because the panel is **not** a `[role=dialog]` node. |
| O109 | usability | US-XCUT · AUTH · deep-link | **A deep link followed while logged out never comes back.** `/ru/learner/assessments` correctly bounces to `/ru/login`, but `role-guard.tsx:38` does `router.replace('/login')` with no capture of the intended path, and the login page always sends you to `getPostLoginPath(role)`. So the whole URL — including the query string — is discarded: `/uz/learner/assessments?play=<id>` verified logged-out → `/uz/login` with `?play` gone. Sharpest case is the GAME `?play` deep link a tutor shares while a session is live, which is time-sensitive; the student still recovers via the assessments list, which is why this is an O and not an F. Kept out of the F ledger deliberately — adding a return path is a missing feature, and enhancements are forbidden as findings. | R2026-08-25f | open (design call) |
| O110 | data-quality | US-XCUT · AUTH · register | **`POST /auth/register` accepts a whitespace-only name and an unbounded one.** `{"name":"   "}` → **201**, and a 600-character name → **201**; the first renders as a blank display name across the sidebar, dashboard greeting and avatar initials. Same shape as **#34** (org name accepts whitespace-only and unbounded length) on the user's own name field, so it is likely one shared validation gap rather than two. Everything else on the register boundary holds: password ≥ 8 and email format are enforced client- *and* server-side, duplicate email is 409, invalid join code is 404 **and leaves no orphaned account**. | R2026-08-25f | open |
| O111 | a11y | US-XCUT · AUTH · login | **The login failure message is visible but never announced.** "Email yoki parol noto'g'ri" renders as ordinary text with **0** `[role=alert]` nodes on the page, so a screen-reader user submitting a wrong password gets no indication that anything happened. Distinct from the #17 silent-failure family — the message *is* rendered, it simply is not in a live region. Same class as O102, O106, O107. **Checked and healthy:** wrong password and a wholly unknown email return the identical message, so there is no user enumeration. | R2026-08-25f | open |
| O112 | data-quality | US-IND · video | **A video segment's stored title can be the source filename.** Segment 3 of the `uz-math.pdf` video is titled `"uz-math.pdf 4"` while every sibling has a real topic title ("Pifagor teoremasi: 3-4-5 uchburchagi", "Asosiy xulosalar"). Harmless today — that segment renders as a quick-check card whose title is never displayed — which is exactly why it stays an O. It would surface the moment a chapter list, transcript or share card renders segment titles. | R2026-08-25f | open |
| O113 | a11y | US-IND · video · quickCheck | **The video quick-check marks your answer by border colour alone.** Clicking the correct option flips its class from `border-zinc-*` to `border-emerald-*` and reveals a correct worked explanation, but `aria-pressed` is `null` and no text says "correct". **This NARROWS O106 rather than repeating it:** the video player *does* grade and explain, so "a quick-check never says whether you were right" is specific to the slide deck — which now has a working reference implementation to copy. What both share is conveying correctness through colour. | R2026-08-25f | open (design call) |
| O115 | availability | US-XCUT · AUTH · rate-limit | **Login's rate-limit bucket is shared by everyone behind one IP.** `loginRateLimit` (`rate-limit.middleware.ts:9-16`) is 30 failed attempts per 15 min with **no `keyGenerator`**, so it keys on IP; once tripped, **correct** logins from that IP are refused too (verified: first 429 at attempt 31, then a known-good credential → 429). For a product whose users are Uzbek classrooms sharing one NAT address, 30 failed attempts across a class is reachable from ordinary typos, and one visitor can lock out a whole school for 15 minutes with 31 requests. The codebase already recognises the pattern — `answerCheckRateLimit:32` keys by **user id** with the comment *"a classroom behind one NAT IP must not share a bucket"* — but login was not given the same treatment. Deliberately **not** filed as a finding or an advisory: IP-keyed login limiting is standard practice and everything around it is done right (see below), so calling it a defect would be crying wolf. Recorded so a human can decide whether login should degrade to a per-account bucket once the IP bucket trips. **Checked and healthy, do not re-test:** `Retry-After: 577` plus the full `RateLimit-Limit/Remaining/Reset/Policy` set are present (which *narrows* **O93** — the auth limiter has the header the admin one lacks); `skipSuccessfulRequests: true`; and `app.set('trust proxy', 1)` (`index.ts:45`) with nginx forwarding `X-Forwarded-For` (`nginx.conf:25`), so the key is the real client IP and **not** a single shared nginx IP — the far worse variant does not apply. Also healthy: the login page maps 401/403/**429**/other to its own translated strings (`login/page.tsx:58-68`, `auth.tooManyAttempts` present in all three message files), so the server's hardcoded English 429 text never reaches a user — a #27-shaped candidate that dissolved on inspection. | R2026-08-25f | open (design call) |
| O114 | a11y · i18n | US-IND · content chrome | **The first control in the accessibility tree on every content page is an English "Choose File".** The upload `<input type="file">` is `sr-only` (1×1, `visibility: visible`) and sits ahead of the logo, so assistive tech announces the browser's default English label at the top of an Uzbek page. **Checked and dissolved as a layout bug** — it is deliberately hidden, not overflowing — leaving only the missing accessible name. | R2026-08-25f | open |
| O116 | validation | US-ADMIN · subscriptions | **A subscription's period end has no plausible range.** `PATCH /admin/users/:id/subscription` validates the *shape* well — `'not-a-date'` → 400 `Invalid datetime`, a number → 400 type error, an unknown `planCode` or `status` → 400 with the full zod enum list — but accepts **`0001-01-01`** and **`9999-12-31`** with 200, and the admin drawer accepts a date six years in the past with no warning or confirmation. Harmless today only because nothing reads `currentPeriodEnd` at all (**#14**), which makes it a trap: the day period-end enforcement is added, year-0001 rows become instant lockouts and year-9999 rows become permanent grants. Recorded rather than filed because on its own it changes no behaviour. | R2026-08-25g | open |
| O117 | ui-affordance | US-ADMIN · subscriptions | **The subscription drawer's Cancel button is `type="submit"`.** Sitting inside the same `<form>` as Save, it *reads* like a save-in-disguise. **Chased and dissolved:** changing the plan to Pro and then clicking Cancel left the subscription at FREE (confirmed on `/billing/me`), so it is handled and no data is at risk. Recorded only so the next cycle does not spend the same minutes re-deriving it — and because the correct attribute is `type="button"`, which would make the intent obvious to the next reader. | R2026-08-25g | open (dissolved) |
| O118 | ui-affordance | US-ADMIN · tutor-requests | **Approve surfaces server validation as a native `alert('Validation error')`.** The API returns the useful part — `{"errors":{"seatLimit":["Number must be greater than or equal to 1"]}}` — and the panel throws away the per-field detail, showing a modal browser alert with the generic top-level message. The operator is told something is wrong but not what. Recorded rather than filed: the guard works and the data is safe; only the report is lossy. | R2026-08-25h | open |
| O119 | validation | US-ADMIN · tutor-requests | **The seat-limit input is `min="0"` while the server requires ≥ 1.** The browser therefore accepts a 0 the API will always reject (400 *"Number must be greater than or equal to 1"*), and the operator only finds out through O118's generic alert. One character in `tutor-requests/page.tsx`. Server-side the field is otherwise well guarded: `-5`/`0` → ≥ 1, `1.5` → *"Expected integer, received float"*, `999999999999` → ≤ 100000. | R2026-08-25h | open |
| O120 | i18n | US-TENANT · students · CSV | **The CSV import failure text `Name is required` is an untranslated English string on an Uzbek page.** Surfaced while proving F118/#65 — the import result panel is otherwise fully localized (*"Yaratildi 0 · Qayta faollashtirildi 0 · O'tkazib yuborildi 0 · Xatolar 2"*), and then each failing row explains itself in English. The message comes straight from the API (`tenant/students.ts:346`), so localizing it means either sending a code the client can translate or translating server-side. Same family as **#37**. | R2026-08-25i | open |
| O93 | standards | US-ADMIN-10 · rate-limit | Admin API rate-limit 429 responses carry **no `Retry-After` header** (RFC 6585 recommends it on 429). Verified: 135-request burst on `/admin/analytics/summary` → exactly 120×200 then 15×429 (bucket 120/60s enforced precisely, first 429 at #121), but `retry-after: null`. Clients can't know when to retry. Admin-only internal endpoint + in-memory per-process bucket → minor standards polish, morning review. Not filed as F. | R21 | open (low-confidence) |
| O92 | data-consistency | US-ADMIN-10 · analytics | Admin MRR/revenue panel prices plans from a **hardcoded USD table** (`config/usage-pricing.ts PLAN_MONTHLY_PRICE_USD`: Team $49, Pro $10, School $149) that has **drifted from the real so'm prices** shipped in `aaaa2b9c` (`lib/pricing.ts`: Team 349 000 so'm ≈ $28, Pro 119 000 ≈ $9.5 ✓, School 1 190 000 ≈ $95). Pro roughly matches but **Team ($49 vs ~$28) and School ($149 vs ~$95) are ~1.5–1.7× overstated**, so admin MRR is inflated (Team $245 vs ~$140 real). Deliberately-separate internal USD gauge (comment: "pricing can change without a schema change"; no exchange rate defined; billing is manual) → defensible-but-drifted, product decision whether to reconcile. User-visible in the revenue table ("Team TENANT 5 $49.00 $245.00"). Not filed as F (structural/by-design-adjacent). | R21 | open (low-confidence) |
| O91 | data-consistency | US-LEARNER · dashboard | Learner dashboard "O'rtacha test" (avg quiz) card shows **"—"** — sourced from `/learner/summary.avgQuizScore` (=**null**) — while `/learner/progress.avgAccuracy`=**100** for the SAME learner (teststudent1) and the same dashboard shows a GAME attempt "Oxirgi 100% · 3265 ball" + an **earned** 🎯 "Birinchi mukammal test" badge. The two "quiz performance" figures disagree because `summary.avgQuizScore` counts only written/practice quizzes while the 100% was a GAME assessment → a student sees "average test: —" despite a visible perfect result. Reproducible + two named oracle endpoints, but definitionally ambiguous (does a GAME count as a "test"?) → product decision, morning review. Not filed as F (structural/subjective). | R21 | open (low-confidence) |
| O90 | usability | US-IND · pricing | Public `/pricing` (+ landing `#pricing`) tier CTAs are hardcoded `href="/register"` for **all** roles (`pricing-tiers.tsx:138`). For a **logged-in** user this dead-ends: INDIVIDUAL "Pro olish" → `/register` → RoleGuard bounces to `/dashboard`, no upgrade flow reached (verified live R21 C1); OWNER "Repetitor bo'lish" would bounce to `/tenant/dashboard`. Real upgrade path is elsewhere (quota-402 modal / settings). Consequence of the intentional manual-billing "all CTAs → register" design (`lib/pricing.ts` comment) → by-design-adjacent. Role-aware CTA (upgrade modal for logged-in) would remove it. | R21 | open (low-confidence) |

| O94 | trust-UX | US-ADMIN · health | With the API unreachable, the red **"CAN'T REACH THE API … stale snapshot"** banner renders correctly (verified), but the group cards **below** it still show unqualified green **"healthy"** pills and `OK` rows from the last good poll. A reader who scrolls past the banner sees green while the backend is down — the page's whole premise is *no false comfort*. Mitigated by the explicit staleness sentence naming the snapshot's age, so not filed as F. Candidate fixes: desaturate stale content, or suffix group pills with "(stale)" while `isError`. | R2026-08-23a | open (low-confidence) |
| O95 | copy-precision | US-ADMIN · health | `rag-indexes` check reports **"All four RAG indexes present"**, but only **three** of the four are indexes (`Chunk_embedding_hnsw`, `Chunk_tsv_gin`, `ContentFigure_embedding_hnsw`) — the fourth is the generated **column** `Chunk.tsv` (oracle-confirmed via `pg_indexes` + `information_schema.columns`). Wording is imprecise, not wrong in effect; a DBA reading it would look for a fourth index that doesn't exist. | R2026-08-23a | open (docs/copy) |
| O96 | visual | US-ADMIN · health | At **390px** the verdict card's meta block (`Fast pass · Nms` + the deep-check hint) keeps `text-right`, producing a ragged left edge in the narrow single column. No overflow (`scrollWidth == clientWidth == 390`, zero over-wide elements) and fully legible — pure aesthetics. Suggest `text-left sm:text-right`. | R2026-08-23a | open (cosmetic) |
| O97 | untested-path | US-ADMIN · health | **RESOLVED R2026-08-23b** — run end-to-end on local (real qa-admin, audit row verified 4→5) *and on production* (all 4 configured providers returned real completions; no account out of credit). Cell now oracle-verified; surfaced F84. Original note: The **deep check** (`POST /admin/health/deep`, real metered provider calls + audit-log write) was **not exercised** this run — deliberately, to avoid provider spend and because the earlier prod probe used a synthetic `userId` that would fail the audit-log FK. Cell `admin.health/ADMIN/deep-check` left at depth `viewed`, staleness ∞ — **top of next run's queue**; must be driven as a real admin so the audit row writes. | R2026-08-23a | open (coverage-gap) |
| O98 | perf | US-XCUT · layout-stability | The sweep failed `layout-stability` on **11 cells** (learner dashboard CLS **0.276**, tenant students **0.272**, limit 0.25). Re-measured live on `/uz/learner/dashboard` with layout-shift attribution: **CLS 0.19 — under the limit**, so it straddles the threshold rather than reproducing on demand. The shift is real though: at t≈312ms a ~178px block is inserted above the fold and pushes `DIV.relative.overflow-hidden` (y 77→255), the stat grid (273→451) and the card below (474→652) down together, then again at t≈328ms — i.e. content mounting after data arrives without reserved space, not font swap. **S3-shaped and non-reproducible above threshold → O, not F** (per §E, only S1/S2 non-repros become flaky Fs). Worth re-measuring against a **production build** before acting: dev-mode CSS/font delivery inflates CLS, so the 0.25 breach may not exist in the artifact users get. **Narrowed R2026-08-25b:** the tenant-students half of this was reproduced 3/3 at 0.289 with a single named cause and split out as **F92** (#44). What remains under O98 is the learner-dashboard family, which still straddles: hand-measured 0.139 (uz-light) and 0.19 last cycle, but 0.316 in the same-cycle sweep — variance, not a stable breach. A **production-build** measurement is still the right next step. Six of the original 11 cells were not product at all: they were redirect cells whose CLS belonged to the destination page, and the probe was fixed in 8a37531c to stop measuring them. **Resolved R2026-08-25e — the production build was finally measured, and it does not rescue any of the three routes.** `next build` + `next start` (`NODE_ENV=production`) on :3100, real form login, 3 cold loads each: `/tenant/students` **0.386 / 0.387 / 0.386** (vs 0.289–0.339 on dev — *worse*, and the +38 px source matches F92's seat pill exactly, so #44 loses its "dev may be inflating this" hedge); `/tenant/assessments` **0.302 / 0.312 / 0.235** with a single named 474 px cause → split out as **F103** (#51); `/learner/dashboard` **0.159 / 0.315 / 0.107** — still straddling on a production build, 1 of 3 loads over the sweep limit and all 3 over Google's 0.1, from a *cascade* of 3–4 small events (hero y 77→255→373→393) rather than one named box. So the learner half stays an O: real but unstable and without a single fixable cause. The deferral "measure against a production build first" is **discharged** — no future cycle should re-open on that ground. | R2026-08-25a | R2026-08-25e: production-build measurement done; students→#44 (hedge removed), assessments→**F103**/#51, learner dashboard remains open (S3, straddling, no single cause) |
| O99 | perf | US-OWNER · materials | Next.js dev warning on `/[locale]/tenant/materials` (4 cells): the YouTube thumbnail `img.youtube.com/vi/…/mqdefault.jpg` "was detected as the Largest Contentful Paint" and should carry `priority`. Dev-only `warn-once` hint, absent from production builds, so it is waived in `console-baseline.json` rather than obeyed blindly — **which row is actually the LCP element depends on the list**, so adding `priority` to every thumbnail could hurt more than it helps. Recorded so the hint is judged rather than lost. | R2026-08-25a | open (perf-hint) |
| O100 | product-gap | US-OWNER · assessments | **An assessment can never be deleted or unassigned.** `apps/api/src/routes/tenant.routes.ts` exposes `POST /tenant/assessments` and `POST /tenant/assessments/:id/assign` but **no DELETE and no unassign** — `DELETE /tenant/assessments/:id` → 404 (route does not exist), and `assign` with `learnerIds: []` is rejected by validation (`"Array must contain at least 1 element(s)"`), so there is not even an empty-set escape. Content *does* have both (`tenantContent.delete('/:id')`, `DELETE /tenant/assignments`), so this is an asymmetry rather than a deliberate append-only design. Consequence: a tutor who publishes a wrong assessment, assigns it to the wrong class, or makes a typo in the title has no remedy, and the students see it on their dashboard permanently. Discovered while trying to clean up a probe assessment this run — which is consequently **still present in the QA tenant** ("QA RaceProbe MaxAttempts"). Filed as O not F because "missing endpoint" may be an unimplemented feature rather than a defect — a product call. **Second instance R2026-08-25b:** messages have the same gap — deleting a sent message 404s, no route exists. Probe messages sent while driving the messaging thread to depth 3 are consequently permanent in the QA tenant. Two independent surfaces now, so this reads less like an unimplemented feature and more like a missing convention. | R2026-08-25a | open (product-decision) |
| O101 | ai-grounding | US-IND · quiz · oracle | **One quiz question out of five is not grounded in the material it cites.** Q1 of the uz-math.pdf quiz asks what kind of angle sits opposite the hypotenuse; the keyed answer (`to'g'ri burchak`) is *factually* right, but neither it nor the explanation appears anywhere in the source section — and the attached `sourceQuote` ("Pifagor teoremasi to'g'ri burchakli uchburchakning tomonlari orasidagi bog'lanishni ifodalaydi.") does not support the claim it is cited for. The other four questions quote the source verbatim and are exact (a≠0, D=0 → one root, D from the quadratic, Venn → circles), no trap answer from the fixture appears anywhere, and the Uzbek is clean (Latin, consistent apostrophes, no leakage). Logged as **O not F** because it is a single stochastic sample — n=1 question in one generated quiz — and the answer is not wrong, only its citation. Worth re-checking across several freshly generated quizzes before anyone changes a prompt. | R2026-08-25b | open (low-confidence) |
| O102 | a11y | US-OWNER · assign · a11y | **The assign result is silent to assistive technology.** `/uz/tenant/materials/[id]/assign` has **0** `[aria-live] / [role=status] / [role=alert] / output` elements, so checking a student, pressing Tayinlash and watching the counter go 1/5 → 2/5 produces no announcement — a screen-reader user gets no confirmation the assignment happened. Same class as **F90/#42** (the GAME player) but a different component, and arguably easier here: an assign is one discrete event, not a per-second countdown, so a polite status region would not chatter. Kept as an observation rather than an F because *what* to announce (the count? the student's name? both?) is a design decision, and the page otherwise passed a full keyboard audit (28/28 focusables reachable, focus visible on all, every checkbox labelled). Pairs with F99, fixed in the same pass. | R2026-08-25c | open (design call) |
| O103 | data-format | US-OWNER · students · export↔import | **The app's own student export cannot be re-imported.** Export writes localized headers (`Ism,Email,Holat`, verified R2026-08-25b); import recognises only `name`/`email`/`username`, so `hasHeader` is true (it matches on `Email`) but `indexOf('name')` is −1 and **every row fails with `"Name is required"`** — a confusing error on a file that visibly has a name column. Not filed as an F: nothing claims export output is an import template, and treating a localized human-readable report as a machine format is a product decision, not a defect. But it is the most likely way a real tutor reaches the F98/#48 code path, so the two should be considered together. | R2026-08-25c | open (product decision) |

## B.3 — Story index rows (tick under each area in the backlog index)

### INDIVIDUAL (B2C) — add
- [ ] US-IND-26 Practice generator v2 (count × types × depth, Mixed default, fill-to-count) · P0
- [ ] US-IND-27 Fill-to-count guarantee + per-type count variance · P0
- [ ] US-IND-28 SRS flashcard session (SM-2, 4-level, Again re-queue, state-leak guard) · P0
- [ ] US-IND-29 In-practice flashcards · P0
- [ ] US-IND-30 Elo-KT mastery (up/down, live update, streaks, concurrency) · P1
- [ ] US-IND-31 Study-mode toggle (semantics + persistence) · P1
- [ ] US-IND-32 Text-selection Ask-AI seed (desktop-vs-mobile, F63 guard) · P1
- [ ] US-IND-33 Section-rail hierarchy nav · P1
- [ ] US-IND-34 Podcast transcript sync (click-seek, rescale, legacy fallback) · P1

### TENANT_OWNER — add
- [ ] US-OWNER-18 Mastery-by-topic on tenant progress · P1
- [ ] US-OWNER-19 Assessment builder — all 8 types round-trip · P0
- [ ] US-OWNER-20 Due-date enforcement matrix (server-side 403) · P0
- [x] US-OWNER-21 GAME-live lifecycle (schedule→go-live→end-live, concurrent learner) · depth 3 R2026-08-25c · two-actor; live does NOT bypass `maxAttempts`; go-live refused on WRITTEN; F97 filed
- [ ] US-OWNER-22 Messaging (owner side) + IDOR · P0
- [x] US-OWNER-23 CSV import (valid/malformed/dup/seat-limit) · depth 3 R2026-08-25c · 10 file shapes; F98 filed (`;` header→student); seat race re-confirms F87 (advisory, not a new finding)
- [ ] US-OWNER-24 CSV export escaping / formula-injection · P0
- [ ] US-OWNER-25 Material detail + per-part generate/retry/fail · P1

### TENANT_LEARNER — add
- [ ] US-LEARNER-14 Structured question players + grading truth-tables + a11y · P0
- [x] US-LEARNER-15 GAME live banner + `?play` deep-link · depth 3 R2026-08-25c · both render and work once loaded; F97 = they never reach an already-open page
- [ ] US-LEARNER-16 GAME structured types under timer + timeout races · P0
- [ ] US-LEARNER-17 Quiz review / strict result breakdown · P0
- [ ] US-LEARNER-18 Messaging (learner side) + IDOR · P0

### ADMIN (3001) — add
- [ ] US-ADMIN-08b Impersonation lifecycle + token-abuse matrix · P0 (S1)
- [ ] US-ADMIN-10 Analytics dashboard (8 endpoints, empty-DB, fuzz, 429) · P1
- [ ] US-ADMIN-11 Approve/flag effects visible to end users + content detail · P0

### XCUT (cross-cutting) — add
- [ ] US-XCUT-22 KaTeX in every player · P1
- [ ] US-XCUT-23 `/terms` + `/pricing` i18n + CTA-by-role · P2
- [ ] US-XCUT-24 TypeBadge i18n on content grids · P2
